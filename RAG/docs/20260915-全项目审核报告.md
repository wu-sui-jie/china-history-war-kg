# 20260915 全项目审核报告（文档与代码一致性）

> 本报告是一次全项目审核的产物，用途是作为**整改工作单**交给执行方（人或模型）。
> 审核时间：2026-09-15。审核方式：四路并行核对（后端链路 F02–F06、前端 F01/F07/F08、
> 离线流程 F09/F10/F11、主文档与测试），全部结论均给出 文件:行号 证据。
> 执行方完成整改后，请在每条问题前的 `[ ]` 中勾选 `[x]`，并遵守文末「执行约定」。

## 一、总体结论

11 个功能模块（F01–F11）中 **9 个「已完成」声明属实**，2 个存在实质缺口
（F07 缺一个完整模块，F09 治理深度弱于文档描述）。

- 全部 **140 个测试用例通过**（`E:/anaconda/envs/AI_Agent/python.exe -m pytest tests/ -q`，7 秒，0 失败）。
- 数据产物规模与文档声称逐一吻合：9,544 条切分 / 1024 维向量 / 39 条题库（reviewed-2）/ 39 条人工评分。
- 抽查 7 项历史整改（T2 软偏置、R4 朝代后缀、T3 类别列、T4 失败分桶、T7 证据 ID、
  T3(v5) 向量接线、T1 demo 死配置清理）**全部在代码中真实落实**，变更记录可信。
- 未发现硬编码假数据或未完成的占位代码。占位机制仅存在于 F11 向量索引构建的
  无密钥降级路径（`data/index/build.py:34-52`），在线侧会被
  `TextSearcher._load_vector_backend` 识别并自动降级关键词，不会产出假向量。
- 主要风险不在「代码烂尾」，而在**文档滞后于代码**：4 处文档与代码直接矛盾、
  6 处文档声称但未实现，集中在 F06 拒答规则、F07 关系路径、F09 治理深度、
  data-contract.md 的 fusion 事件描述。

### 审核中确认无误的重点声明（执行方不要误改）

| 文档声明 | 核实结果 |
| --- | --- |
| F04「关键词+向量+hybrid 融合，默认 hybrid/rrf，百炼 v4 + Chroma」 | 属实（部署级，见 P1-14 关于代码级兜底的备注） |
| F06「真实 LLM 流式 + 推理增量 + 降级链，无 key 时离线摘要兜底」 | 属实（server/generate/llm_client.py:56-165、server/generate/__init__.py:85-127） |
| F02「LLM 兜底默认关闭」 | 属实（config/settings.py:76、config/defaults.py:68、server/runtime.py:103-110） |
| F05「panel 唯一装配方」 | 属实（server/fusion/panel_builder.py，F06/F07 只消费） |
| F11 索引产物 9,544 条 / 1024 维 | 属实（chunks.jsonl 9,544 行、manifest、npy 体积三者互相印证） |
| F10 题库 39 条 reviewed-2、39 条人工评分 | 属实（questions.jsonl 全 reviewed=true；run_20260913_postaudit/scores.jsonl 39 行全 graded） |

## 二、模块完成度总表

| 模块 | 文档声明 | 实核结论 | 依据摘要 |
| --- | --- | --- | --- |
| F01 问答主界面 | 已完成 | ✅ 已实现 | 验收 1–5 全部有代码支撑（SSE 消费、筛选、会话持久化、实体纠正、引用跳转） |
| F02 实体识别消歧 | 已完成 | ✅ 已实现 | 词典优先 + LLM 兜底默认关闭；回归测试齐备；指代消解为简化版 |
| F03 图谱检索 | 已完成 | ✅ 已实现（2 处偏差） | 策略映射、两跳路径、孤立节点防护齐全；「默认 2 跳」未落地、UNKNOWN 只查主实体（见 P1-12） |
| F04 文本检索 | 已完成 | ✅ 已实现 | 三模式 + hybrid/rrf + Chroma + 百炼 v4；and_or 细节文档略滞后（见 P2-19） |
| F05 融合重排 | 已完成 | ✅ 已实现（文档 2 处滞后） | 权重/去重/引用编号/panel 装配齐全；timeline 时间分组未实现（见 P1-10）、冲突判定收窄未回写（见 P2-20） |
| F06 溯源回答 | 已完成 | ⚠️ 大部分实现 | 流式/thinking/降级链/离线摘要属实；拒答规则 4 未实现（P0-1）、score 阈值矛盾（P0-2） |
| F07 知识面板 | 已完成 | ⚠️ 约 90% | **「关系路径」模块无任何代码（P1-5）**；节点追问以按钮变通（P1-11） |
| F08 演示模式 | 已完成 | ✅ 已实现 | 脚本→接口→数据→前端四层闭环；文档残留「暂缓编写」（P2-17） |
| F09 数据治理 | 已完成 | ⚠️ 部分实现 | 主体链路完整，但 apply_audit 零执行（P1-9）、补边/同义合并/定量对比未做（P1-6/7/8） |
| F10 效果评测 | 已完成 | ✅ 已实现（口径注意） | 全链路落地；审核与评分主体是 AI 代理而非人工，文档已如实声明（见 P2-22） |
| F11 切分索引 | 已完成 | ✅ 已实现 | 五条验收全部有代码与产物双重证据，无实质缺口 |

## 三、问题清单（整改工作单）

优先级说明：
**P0** = 文档与代码直接矛盾，会误导对接方或使用方，优先修复；
**P1** = 文档声称但未实现，需补代码或把文档口径降级到实际状态；
**P2** = 文档自身质量问题（滞后、残留、口径漂移），改文档即可。

每条问题按「位置 / 文档现状 / 代码现状 / 修复建议」给出。修复建议若给了
「方案 A / 方案 B」，执行方选其一即可，但同一问题不得同时改两边造成新的不一致。

### P0 文档与代码直接矛盾（4 条）

#### [x] P0-1 F06 拒答规则 4：模型自拒未返回 refused

- 位置：`docs/features/06-grounded-answer.md`（拒答规则第 4 条）；
  代码 `server/generate/__init__.py:99-100`
- 文档现状：承诺「大模型自判无法回答时按拒答路径返回，并输出 finish_reason=refused」。
- 代码现状：LLM 返回任何文本（含「依据现有资料无法确认」类自拒回答）都返回
  `FinishReason.NORMAL`，无对模型拒答文本的识别逻辑；前端无法据此把此类回答标记为拒答。
- 修复建议：
  - 方案 A（补代码）：在 `server/generate/__init__.py` 生成结果返回前，对模型输出做
    自拒文本识别（可复用 `server/generate/refusal.py` 的词表思路），命中则置
    `finish_reason=refused`；需同步在 `tests/` 增加守护测试，并确认
    `server/sse.py:326-330` 缓存策略对 refused 的处理符合预期（当前仅 normal/refused 入缓存，兼容）。
  - 方案 B（改文档）：把该条口径改为「模型自拒以正文文本形式呈现，finish_reason 保持 normal」，
    并在 `server/generate/refusal.py:9` 的模块注释同步说明。

#### [x] P0-2 F06 拒答规则 3：「score 不作为拒答阈值」已被代码推翻

- 位置：`docs/features/06-grounded-answer.md`（拒答规则第 3 条）；
  代码 `server/sse.py:227-230`、`config/settings.py:98`（`vector_refusal_min_score` 默认 0.25）
- 文档现状：写明「score 不作为拒答阈值」。
- 代码现状：vector/hybrid 模式下 `VECTOR_REFUSAL_MIN_SCORE=0.25` 参与拒答判定
  （top_score 低于阈值且实体无共享词时拒答）。
- 修复建议：改文档（推荐）。把第 3 条改写为「score 不单独作为拒答阈值；向量/hybrid 模式下
  作为低分兜底信号之一（`VECTOR_REFUSAL_MIN_SCORE`，默认 0.25），仅与实体不相关信号叠加生效」，
  并在 `.env.example` 对应注释处确认口径一致。

#### [x] P0-3 data-contract：fusion 事件 payload 与代码不一致

- 位置：`docs/data-contract.md:379`；代码 `server/sse.py:196-202`
- 文档现状：声明 fusion 事件 payload 为 `{evidence: [...], citation_index: [...], conflicts: [...]}`，
  即携带完整证据数组。
- 代码现状：实际发送 `{"evidence_count": <条数>, "conflicts": [...], "citation_index": [...]}`，
  只携带数量。对接方按文档解析会取不到证据。
- 修复建议：
  - 方案 A（改文档，推荐）：把 fusion 事件 payload 描述改为 `evidence_count` 现状
    （完整证据由后续 citations/panel 事件承载）。
  - 方案 B（改代码）：若确需 fusion 事件携带完整证据数组，修改 `server/sse.py` 并同步
    前端 `frontend/src/stores/session.ts` 的消费逻辑与 `tests/test_sse_chunking.py` 相关断言。

#### [x] P0-4 deploy：冒烟「证据数达标」断言未实现

- 位置：`docs/deploy.md:109-110`；代码 `scripts/smoke_deploy.py:191-199`（实现）、
  `scripts/smoke_deploy.py:8`（模块 docstring 同样失真）、`:186`（expect 仅存报告）
- 文档现状：称冒烟会断言「finish_reason=normal、回答非空、有引用、证据数达标」。
- 代码现状：只校验前三项；示例题的 `expect`（证据数下限）字段仅写入报告，从未参与断言。
- 修复建议：
  - 方案 A（补代码，推荐）：在 `scripts/smoke_deploy.py` 冒烟循环中把示例题的
    `expect` 下限加入断言（低于下限计入失败），docstring 与文档即可保持一致；
    注意 `data/eval/20260915_v1/demo_examples.json` 中各题 expect 值是否仍合理。
  - 方案 B（改文档）：deploy.md 删除「证据数达标」表述，docstring 同步修改。

### P1 文档声称但未实现（10 条）

#### [x] P1-5 F07「关系路径」模块无任何代码

- 位置：`docs/features/07-knowledge-panel.md`（模块清单第 3 项「关系路径」与用户场景 2）；
  代码 `frontend/src/components/panel/`（PanelPane.vue 五个 tab 为：引用证据/实体卡/图谱子图/时间线/地点，无路径视图；panel 目录 grep「path/路径」无命中）
- 修复建议：
  - 方案 A（补代码）：新增 PathView.vue 组件消费 panel 数据中的关系路径
    （需先确认 `contracts/panel.py` 的 PanelData 是否已携带路径数据；若 F05
    `server/fusion/panel_builder.py` 未输出路径字段，则此方案涉及后端，工作量较大）。
  - 方案 B（改文档，推荐）：在 F07 文档中把「关系路径」从模块清单移除或降级为
    「由图谱子图视图承载路径展示」，用户场景 2 同步改写。

#### [x] P1-6 F09 孤立节点补边流程未实现（有意设计）

- 位置：`docs/features/09-data-governance.md:44-46、69-73`（实现思路 5–6 与处理策略第 2 条）；
  代码 `data/snapshot/isolate.py:4-5`（docstring 明示「孤立节点全部保留、本层不做自动补边」）、
  `data/snapshot/20260904_v2/manifest.json:21`（`governance_enable_relation_extraction=false`）
- 现状说明：孤立节点 3,844/9,925（38.73%）全部保留标记 `is_isolated`，无删除、无补边。
  验收第 5 条靠「根本不补边」被动成立。
- 修复建议：改文档（推荐）。把「补边候选 + 来源/置信度 + pending_review」改为
  「孤立节点全部保留并标记，不自动补边；补边依赖后续人工审核（apply_audit）或未来开启
  关系抽取」，与 F09 已声明的现状自洽。

#### [x] P1-7 F09 战争类型「同义写法合并」未做

- 位置：`docs/features/09-data-governance.md:53-54`；代码 `data/snapshot/governance.py:369-371`
- 代码现状：`event_type_map` 为恒等映射占位（27 类原样保留），代码自述「未做自动合并，
  供后续人工增补」；治理报告另标 review_needed 3 类（战争/议和/政治事件）。
- 修复建议：改文档，注明「event_type_map 当前为恒等映射，同义合并待人工增补
  （review_needed 清单见治理报告）」。

#### [x] P1-8 F09「治理前后对比」缺定量汇总

- 位置：`docs/features/09-data-governance.md:84`（验收第 2 条）；代码 `data/snapshot/governance.py:392-396`
- 代码现状：`_before_after()` 仅定性说明；前后信息散落在 data_issues 逐条 before/after
  与孤立/重名/类型分布统计中，无整表定量对比。
- 修复建议：
  - 方案 A（补代码）：在 `governance_report.json` 中增加 before/after 定量汇总节
    （实体数、关系数、朝代修正数、孤立占比等）。
  - 方案 B（改文档）：验收第 2 条口径改为「报告含治理统计快照与 data_issues 逐条
    before/after 明细」。

#### [x] P1-9 F09 apply_audit 人工审核回填零执行

- 位置：`docs/features/09-data-governance.md`（人工审核闭环描述）；
  证据：`data/snapshot/20260904_v2/governance_report.json` 与 `20260915_v1` 快照均无
  `audit_applied` 字段
- 现状说明：回填工具（`scripts/apply_audit.py` → `data/snapshot/apply_audit.py:53-289`）
  功能齐备（add/remove_alias、merge、事件合并需显式确认、决定留痕），但从未在真实数据上执行。
  「人工审核记录」目前只有待审清单（`audit/duplicate_name_groups.json`，1,215 组 / 3,848 条）。
- 修复建议：
  - 方案 A（执行闭环，推荐）：用 apply_audit 跑一次小批量真实回填（例如从
    duplicate_name_groups 里挑若干组做别名补充/合并），产出含 `audit_applied` 的新版本快照。
    **注意**：新快照需重建索引（`scripts/build_index.py`）并更新 `.env` 中数据版本后回归测试，
    工作量较大，执行方评估后再做。
  - 方案 B（改文档）：把验收第 5 条口径改为「审核回填工具就绪；当前人工审核记录为
    待审清单，尚未执行回填」。

#### [x] P1-10 F05 timeline 未按事件时间分组/排序

- 位置：`docs/features/05-fusion-rerank.md`（timeline「按事件时间和朝代分组」的 8 条实现思路）；
  代码 `server/fusion/panel_builder.py:200-224`
- 代码现状：只按朝代分组；时间仅用于把「不详」归入 `UNKNOWN_TIME_LABEL` 置尾；
  组内条目未按时间排序。
- 修复建议：
  - 方案 A（补代码）：在朝代分组内按 start_date 升序排序（「不详」保持置尾），
    属小改动；需核对 `tests/test_panel_map_points.py` 等现有测试是否受影响。
  - 方案 B（改文档）：timeline 描述改为「按朝代分组，时间不详置尾，组内按数据原始顺序」。

#### [x] P1-11 F07 节点追问交互与「查看实体详情」未按文档实现

- 位置：`docs/features/07-knowledge-panel.md`（模块清单第 7 项「点击图谱节点追问，
  弹出『继续提问该实体』『查看实体详情』操作菜单」、模块展示策略 2）；
  代码 `frontend/src/components/panel/SubGraphView.vue:17-19、64-66、86-96`、
  `PanelPane.vue:14、34-39`
- 代码现状：追问通过图下方节点按钮列表触发（askNode 已实现）；echarts 画布内节点
  click 未绑定事件；无「查看实体详情」入口；无按问题类型自动切换 tab 的逻辑
  （每轮新消息重置回 evidence tab）。
- 修复建议：
  - 方案 A（补代码）：给 echarts graph 绑定节点 click 事件调用 askNode
    （小改动）；「查看实体详情」与自动切 tab 可先不做。
  - 方案 B（改文档）：模块清单第 7 项改为「节点追问（按钮列表形式）」；
    模块展示策略 2 标注「未实现自动切换，标签页手动切换」。

#### [x] P1-12 F03「默认 2 跳」未落地 + UNKNOWN 策略只查主实体

- 位置：`docs/features/03-graph-retrieval.md`（待确认问题说明 1「初版建议默认 2 跳」；
  unknown 策略「实体 1 跳查询作为候选」）；代码 `server/graph/search.py:121-140、213-215`、
  `server/graph/query_strategies.py:273-280`
- 代码现状：除 event_event 双事件两跳路径外，其余策略均 1 跳邻接；UNKNOWN 只查
  第一个事件实体的 1 跳（多实体时其余实体被丢弃）；comparison 与 relation 共用同一实现。
- 修复建议：改文档（推荐，检索深度已经过 F10 评测定档）。把「默认 2 跳」建议标注为
  「未采纳，深度经 F10 评测按 1 跳定档，event_event 例外走两跳路径」；unknown 策略
  描述改为「主实体 1 跳查询作为候选」。

#### [x] P1-13 F06 RAGv5 新增能力未回写功能文档正文

- 位置：`docs/features/06-grounded-answer.md` 正文；代码
  `server/generate/refusal.py:17-28、55-73`（领域外谓词拒答词表与放行逻辑）、
  `server/generate/llm_client.py:99-115`（thinking/reasoning 双端兼容）、
  `server/generate/__init__.py:85-98`（空正文降级，注释含 v5 实测依据）
- 修复建议：改文档。在 F06 文档补充三节：领域外谓词拒答（词表 + 证据含词放行 + 编排位置
  `server/sse.py:241-252`）、推理增量（thinking 事件）、空正文/错误降级路径。

#### [x] P1-14 F04「默认 hybrid/rrf」依赖 .env，代码级兜底是 keyword

- 位置：`.env:24、26`（TEXT_MODE=hybrid / TEXT_HYBRID_STRATEGY=rrf）；
  `config/defaults.py:47`（`TEXT_MODE = "keyword"`）、`config/settings.py:93/95`
  （dataclass 兜底 keyword/weighted）
- 现状说明：部署级默认 hybrid/rrf 属实，但换环境丢失 .env 时会静默回退关键词模式。
- 修复建议：
  - 方案 A（改代码，推荐）：把 `config/defaults.py:47` 与 `config/settings.py:93`
    兜底值对齐为 hybrid/rrf（strategy 已是 rrf），消除静默回退；改动后跑全部测试。
  - 方案 B（改文档）：在 F04 文档与 deploy.md 环境变量表标注「代码级兜底为 keyword，
    生产部署必须配置 .env」。

### P2 文档自身质量问题（8 条）

#### [x] P2-15 architecture.md 目录树滞后

- 位置：`docs/architecture.md:37-51`
- 问题：目录树只列 docs、data、server、frontend、scripts，漏 `contracts/`（11 文件）、
  `config/`（settings.py + defaults.py）、`evaluation/`（6 文件）、`lib/`（3 文件），
  而正文「统一接口与契约」一节依赖数据契约。
- 修复：更新目录树补齐四个目录及一句话职责说明。

#### [x] P2-16 默认模型名口径漂移

- 位置：`docs/architecture.md:31`（deepseek-v4-flash）、
  `docs/features/06-grounded-answer.md`（同名）；代码 `config/defaults.py:63`
  （`deepseek/deepseek-v4.1-flash`）、`docs/deploy.md:67`（已正确）
- 修复：把两处旧模型名统一为 `deepseek/deepseek-v4.1-flash`（中转 id）。

#### [x] P2-17 F08 文档残留「暂缓编写」过期提示

- 位置：`docs/features/08-demo-mode.md:10`
- 问题：「本功能暂缓编写」与该文档已完成状态自相矛盾（RAGv5 已交付）。
- 修复：删除该行；同时检查第 4 条「输入：演示模式开关」——代码无开关，示例区常驻
  （`config/defaults.py:79` 注释「无 DEMO_MODE 开关——示例区恒显示」），
  把该输入项改为「无需开关，欢迎页常驻展示」。

#### [x] P2-18 RAGv5 整改方案测试数字快照滞后

- 位置：`docs/RAG_v1/RAGv5-遗留项整改方案.md:19`（「134 全过」）
- 现状：当前 140 个用例（其后新增 test_panel_map_points.py 等）。
- 修复：更新为「140 全过（截至 2026-09-15）」或加注快照日期。

#### [x] P2-19 F04 and_or 模式细节未回写

- 位置：`docs/features/04-text-retrieval.md`（keyword_mode and_or 描述）；代码
  `server/text/searcher.py:222-227`
- 问题：实现为「AND 命中不足 3 条即并入 OR 结果」，比文档「AND 优先、OR 兜底」更激进
  （代码注释引 RAGv5 §2.5-2，功能文档未同步）。
- 修复：F04 文档补注该细节口径。

#### [x] P2-20 F05 冲突判定范围收窄未回写

- 位置：`docs/features/05-fusion-rerank.md`（different_object 冲突描述）；代码
  `server/fusion/conflict.py:62-68`
- 问题：实现只对 field_map 中 exact 单值组（发起方/防守方）关系判定冲突，多值关系
  （主帅/将领）不报冲突（有意防误报，代码注释「待 F10 评测再调」）。
- 修复：F05 文档补注「different_object 仅对单值关系生效」。

#### [x] P2-21 data-contract SSE payload 超集未记录

- 位置：`docs/data-contract.md:370、375`；代码 `server/sse.py:111-120、149、182-183`
- 问题（轻微，超集不破坏对接）：entities 事件实际还带 `question_type`、
  `rewritten_question`、`dynasty_bias`、`elapsed_ms`；done 命中缓存多 `cache_hit`；
  graph_results实际带 `hit_entities`。文档未同步。
- 修复：data-contract.md 对应事件字段表补齐。

#### [x] P2-22 F10 评分口径与最新 run 状态备注

- 位置：`docs/features/10-evaluation.md`、`docs/README.md`
- 问题（非缺陷，口径提示）：题库审核与 39 条评分的 reviewer 为
  「AI代理审核-ZCode(deepseek-v4-flash)」，与 F10 文档第 78 行「黄金问答集由人工审核」
  的需求口径存在差；且最新 hybrid/LLM run（`data/eval/20260915_v1/runs/v5_coords_v1`、
  `20260904_v2/runs/v5_llm_*`）尚无人工评分回填，最新链路的正确性结论
  （25 correct / 3 partial / 0 incorrect）评分为 AI 代理口径。
- 修复：在 F10 文档显著位置（不只在状态栏）标注评分主体口径；如需正式口径，
  安排人工复核存疑清单并回填 v5_coords_v1 评分。

### 其他备忘（不强制整改）

- F02 验收第 3 条（错误识别被检索分数控制）无专项代码/测试，靠下游打分隐式满足；
  指代消解为简化版（只还原上一轮首个事件实体，`server/query/understand.py:284`）。
  可不改，若改文档注明「简化实现」即可。
- F03 证据 ID 跨表唯一性已修复（`server/graph/search.py:26-44`，带哈希回退），
  `tests/test_evidence_ids.py` 守护，无需处理。
- `frontend/dist/` 存在 2026-09-15 11:20 的最新构建产物（含地图功能），部署形态就绪。
- 代码超出文档的增强（无风险，不必整改）：分块参数对照实验
  （`scripts/compare_chunking.py` + 两个索引变体）、向量一致性抽检
  （`scripts/check_vector_consistency.py`）、高德坐标工具（`scripts/fetch_place_coords.py`）、
  评分 CSV/XLSX 往返（`scripts/review_bank.py` 的 scores-export/apply）。

## 四、执行约定（交给执行方）

1. **项目红线**：不修改 `backend/`、`frontend/`（指项目根下旧前端）、
   `entity-event-relation/` 中的旧代码；本报告所有路径均在 `RAG/` 子项目内。
2. **文档维护约定**（`docs/README.md`）：功能文档保持统一章节结构；每个功能文档
   「状态」只保留一种当前状态；总览 `docs/README.md` 的功能清单表若因整改需要更新
   状态备注，先改功能文档再同步总览。
3. **改动顺序建议**：先做全部 P2（纯文档，零风险）→ P0-2、P0-3（方案 A 改文档）
   → P0-4、P1-10、P1-11（小代码改动）→ P0-1、P1-14（涉及行为变化，需评审）→
   P1-9 方案 A（涉及重建索引，单独评估）。
4. **验证要求**：任何代码改动后运行
   `E:/anaconda/envs/AI_Agent/python.exe -m pytest tests/ -q`，
   保持 140 个用例全通过；若新增行为（如 P0-1 自拒识别），必须新增守护测试。
   注意：系统 PATH上的 `python` 是 WindowsApps stub（exit 49），必须用上述完整路径。
5. **一致性自检**：同一事实（如默认模型名、TEXT_MODE 默认值、fusion 事件字段、
   冒烟断言项）在 docs/README.md、features/*、architecture.md、data-contract.md、
   deploy.md 多处出现，修改时必须全局 grep 同步，避免改一处漏一处。
6. 完成后在本文件每条问题前勾选，并在文末追加「整改记录」节（日期、改动文件清单、
   测试结果）。

## 五、整改记录

- **整改日期**：2026-09-15
- **测试结果**：`E:/anaconda/envs/AI_Agent/python.exe -m pytest tests/ -q` → **154 passed**
  （整改前 140；新增 `tests/test_panel_timeline_order.py` 6 条、`tests/test_model_refusal.py` 8 条）
- **前端构建**：`cd frontend && npm run build`（vue-tsc 类型检查 + vite build）通过，`frontend/dist` 已更新
- **改动文件清单与理由**：见 [changes/20260915-full-audit-fix-summary.md](changes/20260915-full-audit-fix-summary.md) 第二、三、四节

### 逐条处置摘要

| 编号 | 处置 | 方式 |
| --- | --- | --- |
| P0-1 | 已整改 | 方案 A（补代码）：`refusal.detect_model_refusal` + `generate()` 置 refused + 8 条守护测试 |
| P0-2 | 已整改 | 改文档：F06 拒答规则第 3 条 + data-contract 的 score 口径同步 |
| P0-3 | 已整改 | 方案 A（改文档）：fusion 事件 payload 改为 `evidence_count` |
| P0-4 | 已整改 | 方案 A（补代码）：冒烟脚本断言 `expect.graph_min` / `text_min` |
| P1-5 | 已整改 | 方案 B（改文档）：F07 模块清单第 3 项降级为“由图谱子图承载” |
| P1-6 | 已整改 | 改文档：F09 补边口径（不自动补边，留待人工回填/关系抽取） |
| P1-7 | 已整改 | 改文档：`event_type_map` 恒等映射与 review_needed 注明 |
| P1-8 | 已整改 | 方案 B（改文档）：验收第 2 条改为统计快照 + data_issues 明细口径 |
| P1-9 | 已整改（未执行回填） | 方案 B（改文档）；不执行真实回填与重建索引，理由见整改总结第三节 |
| P1-10 | 已整改 | 方案 A（补代码）：timeline 组内按可解析 start_date 升序 + 6 条守护测试 |
| P1-11 | 已整改 | 方案 A（补代码）：echarts 节点 click 追问；未实现项（详情菜单/自动切 tab）已在文档标注 |
| P1-12 | 已整改 | 改文档：F03 两跳建议标注未采纳、unknown 策略改主实体口径 |
| P1-13 | 已整改 | 改文档：F06 补领域外谓词、推理增量、降级路径三节 |
| P1-14 | 已整改 | 方案 A（补代码）：defaults/settings 兜底对齐 hybrid/rrf（含 3 处文档同步） |
| P2-15 | 已整改 | 改文档：architecture.md 目录树补齐 contracts/config/evaluation/lib |
| P2-16 | 已整改 | 改文档：architecture.md、06-grounded-answer.md 统一 `deepseek/deepseek-v4.1-flash` |
| P2-17 | 已整改 | 改文档：08-demo-mode.md 删“暂缓”行、输入项改常驻；docs/README.md 同步 |
| P2-18 | 已整改 | 改文档：RAGv5-遗留项整改方案.md 测试数字加注 140 快照 |
| P2-19 | 已整改 | 改文档：04-text-retrieval.md 补 and_or 实际口径 |
| P2-20 | 已整改 | 改文档：05-fusion-rerank.md 与 data-contract 补 different_object 单值限定 |
| P2-21 | 已整改 | 改文档：data-contract.md 补齐 entities / graph_results / done 超集字段 |
| P2-22 | 已整改 | 改文档：10-evaluation.md 文首加评分主体口径节，多处条目补注 |

### 未采纳建议与遗留说明

1. **P1-9 方案 A（执行真实回填 + 重建索引）未执行**：人工审核回填的决定主体应是人，
   AI 代挑分组会与“人工审核”语义冲突；且回填后需重建 9,544 条向量索引并切换数据版本，
   属部署级决策，建议单独安排。当前以方案 B 将文档口径降到实际状态。
2. F02 指代消解简化实现等「其他备忘」项按报告“不强制整改”未处理。

### 一致性自检

- 默认模型名：`docs/architecture.md`、`docs/features/06-grounded-answer.md` 已统一为
  `deepseek/deepseek-v4.1-flash`；`docs/deploy.md` 原已正确。
- TEXT_MODE 默认值：`config/defaults.py`、`config/settings.py`、`server/text/README.md`、
  `docs/RAG_v1/RAGv5-规划说明.md` 已同步为 hybrid/rrf。
- fusion 事件字段：`docs/data-contract.md` 与 `server/sse.py` 一致（`evidence_count`）。
- 冒烟断言项：`docs/deploy.md`、`scripts/smoke_deploy.py` docstring 与实现一致。
