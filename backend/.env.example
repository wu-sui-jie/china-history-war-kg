# backend 的本机私有配置模板。
# 复制为 backend/.env 后填入真实值 —— .env 已被 .gitignore 忽略，不会进公开仓库。

# ---- Neo4j（图谱库）----
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# ---- JWT 登录鉴权密钥 ----
# 留空则在进程内随机生成（本地开发可用，重启后旧 token 失效）；
# 生产或多进程部署必须显式设置，且各进程一致。
JWT_SECRET=
