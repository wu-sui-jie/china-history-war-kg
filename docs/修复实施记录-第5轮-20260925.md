# 修复实施记录（第 5 轮，2026-09-25）

> 本轮由审核方执行 [修复工作单-第3轮-20260924收口.md](修复工作单-第3轮-20260924收口.md) 的 **S1-1 凭据轮换**、
> **S2 的 API 级端到端回归**，并顺带修复两项执行中发现的真问题、完成 EER 三项正确性修复（原备忘暂缓项，经确认提前收口）与 S3-5 的样式外移。
> 前序：[第 4 轮](修复实施记录-第4轮-20260925.md) / [第 4 轮审核](修复审核报告-第4轮-20260925.md)。

## 一、S1-1 凭据轮换（四轮悬置的安全敞口，本轮闭环）

- Neo4j：`ALTER USER neo4j SET PASSWORD '<随机新口令>' CHANGE NOT REQUIRED`（首轮用 `CHANGE REQUIRED false` 语法报错，Neo4j 5 的正确子句是 `CHANGE NOT REQUIRED`）；
- `backend/.env` 的 `NEO4J_PASSWORD` 与 `JWT_SECRET`（`secrets.token_urlsafe(48)`）同步原子更新，无断连窗口；
- 验证：新口令连接成功（7470 节点无损）、**旧口令实测失效**、新 JWT 签发/解码正常、后端模块用新 `.env` 连接成功、5000 端口无旧进程（无需重启）。
- 遗留决策项：`git filter-repo` 重写历史（会改写全部提交哈希、需强推）仍待定。

## 二、S2 API 级端到端回归（真实库 + 真实请求，20/20 通过）

一次性脚本（跑完即删）覆盖：鉴权三态（无 token/伪造/过期 → HTTP 401）、`/api/userinfo` 身份不串号、注册新账号建 `viewer`、viewer 调写接口 403、admin 真实创建→更新→删除测试节点（SQLite 与 Neo4j 双侧同步验证，含 `neo4j_id` 回写与删除清理，finally 兜底清理不留痕）、对账接口、`import_json_to_sqlite.py --help`（确认 59 处未定义名修复后可执行）、`sync_sqlite_to_neo4j.py --prune-orphans` 演练（未发现孤儿）。

**浏览器 UI 的点击回归**（弹层样式、SSE 逐字、iframe 渲染）仍需人工，API 覆盖不到。

## 三、顺带修掉的真问题：对账口径误报

端到端回归暴露对账报 `Place mismatch 2433`——逐层排查证实两侧**实际完全一致**（SQLite distinct 名称 1028/2883/1067/2492 与 Neo4j 节点数分毫不差），差异全部来自同名行：SQLite 关系表允许同名行（不同朝代同名地名/事件，重名行 `neo4j_id` 留空属正常），Neo4j 按 (标签, 名称) 唯一合并。`build_sync_reconciliation` 的行数对比口径在重名场景必然误报。已改为 `COUNT(DISTINCT name)` 并在 docstring 记录实测依据；复验四类全部 `ok`、`consistent=True`、workbench `sync_mismatch=0`。

## 四、EER 三项正确性修复（原备忘暂缓项，经确认提前收口）

| 项 | 修复 | 验证 |
| --- | --- | --- |
| recall 公式错误 | `optimal_evaluator.py` 改标准 micro 口径（P=TP/(TP+FP)、R=TP/(TP+FN)）；原按预测数加权的 P 数学上恒等于 micro-P，R 是真正的修正点 | 同一产物对比：micro-P 与旧公式相等（0.1150=0.1150，恒等性实证）；旧公式 R 虚高 **+0.0140** 已纠正；另补 gold 为空时的除零防护 |
| 空结果写缓存 | `entity_extractor` / `event_extractor` 的兜底 `except` 改为异常上抛（含批次失败），`main.py` 既有「失败不落缓存」判定恢复生效；relation 的派生兜底为确定性降级，按设计保留 | compileall + ruff F821 通过；异常路径行为由既有单测与下次重跑覆盖 |
| 产物缺模型名 | `llm_client` 暴露 `base_url`；`save_results` 新增 `llm_meta` 参数，两处 metadata（`9_final_all.json` 与 `published/final.json`）写入 `model`/`api_base` | 临时目录实测：metadata 正确落盘（`deepseek-chat`） |

评估重跑输出 `evaluation/recheck-micro-20260925`（未覆盖 `evaluation/latest` 旧基线）。注意：新旧评估的总差异（pred 4946→8875、F1 0.3072→0.2042）主要来自**产物本身在旧评估之后更新过**，公式修正的净效应仅为 R -0.0140。

## 五、S3-5 第二步（部分）：样式外移

`inference/index.vue` 的 `<style scoped>`（1847 行）原样外移至 `src/views/inference/index.css`，页面改为 `<style scoped src="./index.css">`。页面 **3007 → 1161 行**；`build:check`（vue-tsc 0 错误 + vite build）与组件测试（54 passed）通过。**运行时行为不变**（scoped 语义保留）。

## 六、未做项与原因

| 项 | 原因 |
| --- | --- |
| S3-5 模板拆子组件（352 行模板） | 与 170 处 any 的类型收敛互相牵连，需逐组件定 props/emits 类型，适合独立一轮 |
| S3-5 any 清理（约 170 处） | 同上 |
| 路由蓝图 | 工作单明确「蓝图放最后」；机械搬家但量大（40 路由），需每步跑 67 请求对照，独立立项 |
| S1-2 auth_basic | 部署时动作，配置已就位 |
| S2 浏览器点击回归 | 人工，API 覆盖不到视觉/交互 |
| git filter-repo | 决策项，见第一节 |

## 七、验证证据汇总

| 目标 | 结果 |
| --- | --- |
| Neo4j 新凭据连接 / 旧凭据失效 | 通过 / 符合预期 |
| API 级端到端回归 | 20/20 通过 |
| 对账复验 | 四类 ok、consistent=True、sync_mismatch=0 |
| EER 评估重跑 | micro-P 恒等实证、R 修正 -0.0140 |
| EER metadata 落盘 | model/api_base 写入正确 |
| compileall（3.8）+ ruff F821（backend 与 EER 改动文件） | 通过 |
| 前端 build:check + 组件测试 | 0 错误 / 54 passed |
