# 修复审核报告（第 2 轮，2026-09-24）

> 审核对象：[修复实施记录-第2轮-20260924.md](修复实施记录-第2轮-20260924.md) 及其对应提交（`6148490` → `38c0df7` 共 6 个）。
> 审核依据：[修复工作单-第2轮-20260924复核.md](修复工作单-第2轮-20260924复核.md)、[项目审查与修复清单.md](项目审查与修复清单.md)。
> 审核方式：对照源码逐项抽查声称完成的修复、独立复跑测试套件、核对提交序列与工作区状态；标注「已复核」的条目为本审核直接验证，标注「采信记录」的条目为本机无法复现、采信实施记录的自述验证。
> 第 1 轮的审核结论已并入第 2 轮工作单（其 R1 系列即第 1 轮审核发现），本报告只审第 2 轮。

## 一、审核结论

**通过（有收尾事项）。** 第 2 轮声称的 R1 全部 5 项、R3.1 的 12 项均真实落地，抽查 15 个关键点无一虚报；RAG 测试独立复跑 349 passed 与记录一致；提交拆分清晰、工作区干净（仅剩 feishu-bot 并行工作）。未完成项的理由均成立且已如实记录。

遗留两件事：**R2-1 凭据轮换仍未执行**（唯一安全敞口，人工事项），以及两处文档收尾不一致（见第四节）。

## 二、逐项验证记录

### 2.1 R1 复核回归（第 1 轮审核发现项）

| 项 | 实施记录声称 | 审核方式 | 结果 |
| --- | --- | --- | --- |
| R1-1 开发态接口断链 | vite 代理加正则规则 + 真实环境验证 6 条旧路径 | 已复核：`frontend/vite.config.ts:47` 正则规则在位，覆盖 CRUD 四页、`search_name_kg`、`user/(menu\|permission)` | ✅ 通过 |
| R1-2 CI Python 3.8 | legacy-backend 钉到 ubuntu-22.04 | 已复核：`ci.yml:154` | ✅ 通过 |
| R1-3 孤儿节点与按名回退 | `collect_orphans`/`prune_orphans`（默认演练、`--yes` 才删）；按名回退「同名唯一才动」 | 已复核：`sync_sqlite_to_neo4j.py:232,274` 函数在位；真实 Neo4j 演练结果为采信记录 | ✅ 通过 |
| R1-4 sandbox 措辞降级 | 注释明确「不是安全隔离」 | 已复核：`RagAssistant.vue:23-26` | ✅ 通过 |
| R1-5 提交 | 按模块拆分提交 | 已复核：`git log` 6 个修复提交，工作区仅剩 feishu-bot 10 个文件（并行工作，记录已声明不纳入） | ✅ 通过 |

### 2.2 R3.1 中小修复

| 项 | 验证方式 | 结果 |
| --- | --- | --- |
| BE-6 并发四项 | 已复核：`app.py:77` `_singleton_init_lock`、`rule_llm_integration.py:35` `_ThreadLocalNeo4j`（:1243 接入线程池）、`app.py:1840` `MAX_LOAD_ALL_NODES=3000` 降级加载 | ✅ |
| BE-9 朝代数据收敛 | 已复核：`backend/dynasty_data.py` 存在；「口径差异按原样保留」的决策与 `describe_dialect_gaps()` 设计合理 | ✅ |
| BE-12/BE-13 清理 | 采信记录（OllamaAdapter 提为模块级、7 处 import 上提、README 映射表）；与 BE-6 同提交，抽查未见回归 | ✅ |
| DB-7 字段映射收敛 | 已复核：`backend/node_property_mapping.py` 存在；记录声称做了与 `git show HEAD` 旧实现的逐字段比对并刻意保留历史行为差异——该验证方法正确，结论采信 | ✅ |
| FE-11 重复实现收敛 | 已复核：`utils/graph.ts` 的 `mergeNodeRelations` 在位 | ✅ |
| FE-12 交互一致性 | 已复核：views 下 `alert(` 计数为 0 | ✅ |
| FE-6 死代码（部分） | 已复核：`commone.ts`、`verificationImg`、`loginQrcode` 全部清零 | ✅ |
| RAG-5 日志落地 | 已复核：`lib/logging_util.py:42` `setup_rag_logging`，`api.py:89` 模块级挂载 | ✅ |
| RAG-6 embed 截断改报错 | 已复核：`embeddings.py:75,89` `raise ValueError` | ✅ |
| RAG-9 构建模式防呆 | 已复核：`RAG/frontend/vite.config.ts:23-26` 写标记 + `api.py:868` 启动交叉核对 | ✅ |
| RAG-10 检索词数配置化 | 已复核：`settings.py:243,481` `TEXT_QUERY_MAX_WORDS` | ✅ |
| RAG-7 密钥别名 + 一致性测试 | 采信记录（下划线别名在前、`test_config_consistency.py` 4 条用例）；测试复跑覆盖 | ✅ |
| DOC-8 文档口径 | 采信记录；RAG 测试数统一为 345 基线，本轮复跑 349（+4 新增）与之吻合 | ✅ |

### 2.3 测试与提交

| 目标 | 结果 |
| --- | --- |
| RAG 测试独立复跑（本审核执行） | **349 passed**（20.8s），与记录声称一致 |
| 提交序列 | `6148490`（第 1 轮）→ `5132a1a`（R1 回归）→ `2f50d43`/`61cd290`/`f18b570`（R3.1 三批）→ `38c0df7`（文档回写），信息与代码对应 |
| 工作区 | 仅 `feishu-bot/` 10 个文件未提交（用户并行工作，实施记录已声明不在本轮范围） |

## 三、未完成项的审核确认

| 项 | 状态 | 审核意见 |
| --- | --- | --- |
| **R2-1 凭据轮换** | **未执行** | 本审核实测 `backend/.env` 仍含历史泄露的旧口令（grep 命中）。公开仓库 git 历史中凭据仍可取回，是当前唯一安全敞口。**部署前必做**，人工五分钟：`ALTER USER neo4j SET PASSWORD` + 换 `JWT_SECRET` + 更新 `.env` |
| R2-2 端到端回归 | 部分（dev 代理与真实 Neo4j 演练已做） | CRUD 增删改、viewer 403、RAG iframe、`--yes` 导入等完整人工回归待走一遍；其中 CRUD 链路刚被 R1-1 修过，优先 |
| R3.2 重构 6 项（P2-1/P2-2/P2-5/P2-4/FE-4/FE-7） | 未做 | 理由成立：工作单定位即「先建回归对照再动手」，对照脚本尚未建立。下一轮先建对照基础设施 |
| BE-8 全文索引、FE-6 剩余、FE-13 | 未做 | 理由成立（需真实数据评估 / 与 FE-4 同批 / 属数据治理） |
| R4 entity-event-relation | 按约定暂缓 | recall 公式错误仍影响已产出评估结论，EER 收口时优先 |
| RAG-1 反代鉴权 | 部署侧待启用 | 公网部署前必须开启 `auth_basic`（或 SSO），配置开关已就位 |

## 四、审核发现的问题（文档收尾类）

1. **工作单 checkbox 未回写**：R1-1 ~ R1-5 实际已完成，但 [修复工作单-第2轮-20260924复核.md](修复工作单-第2轮-20260924复核.md) 中仍为 `- [ ]`（`38c0df7` 只回写了实施记录，工作单本体漏勾）。不回写会让后续轮次误判状态。**待实施方勾选。**
2. **第 2 轮实施记录的提交表笔误**：第一节标题写「三个提交」，实际列出 5 个（git 实为 6 个修复提交）。数字与内容不符，顺手更正即可。
3. **feishu-bot 10 个文件未提交**：不在本轮审核范围，但长期滞留工作区会干扰后续审核的「工作区干净」判定，建议尽快单独提交。

## 五、下一步建议（优先级排序）

1. **立即（人工，五分钟）**：R2-1 凭据轮换——这是唯一的安全敞口，且不依赖任何代码改动。
2. **提交前**：回写工作单 checkbox、更正实施记录笔误、提交 feishu-bot。
3. **下一轮（半日内）**：R2-2 完整端到端回归（重点：CRUD 链路、403 拒写、对账页）。
4. **下一轮立项**：先建「响应快照对照」基础设施，再启动 R3.2 重构与 EER 收口（recall 公式优先）。

## 附：本审核执行的验证命令

```bash
git log --oneline -10                          # 提交序列
grep -n "runs-on" .github/workflows/ci.yml     # R1-2
grep -n "proxy" frontend/vite.config.ts        # R1-1
grep -n "collect_orphans\|prune_orphans" backend/sync_sqlite_to_neo4j.py   # R1-3
ls backend/dynasty_data.py backend/node_property_mapping.py               # BE-9 / DB-7
grep -rn "setup_rag_logging" RAG/server/api.py # RAG-5
E:/anaconda/envs/AI_Agent/python.exe -m pytest RAG/tests -q   # 349 passed
grep -c "2004wu1023" backend/.env              # R2-1 未执行的证据（命中 1 次）
```
