# 修复审核报告（第 3 轮，2026-09-24）

> 审核对象：[修复实施记录-第3轮-20260924.md](修复实施记录-第3轮-20260924.md) 及其提交（`f23388e` → `fdf98eb` 共 6 个）。
> 审核依据：[修复工作单-第2轮-20260924复核.md](修复工作单-第2轮-20260924复核.md) 的 R3.2 各项。
> 审核方式：对照源码逐项抽查、独立复跑测试、核对提交与工作单回写；「已复核」为本审核直接验证，「采信记录」为本机无法复现、采信实施记录自述。

## 一、审核结论

**通过。** 声称的对照基础设施、P2-1 第一步（拆报表构建器）、P2-5 第一步（两处 N+1 下推）全部真实落地；工作单 R1 系列 checkbox 已按上轮审核意见补勾；RAG 测试独立复跑 349 passed 一致；`compileall`（3.8）通过。实施方法学成熟（先建兜底再重构、AST 闭包定边界、diff 驱动抓错），无虚报。

## 二、逐项验证记录

### 2.1 已复核通过

| 项 | 验证结果 |
| --- | --- |
| 对照基础设施 | `backend/tools/snapshot_responses.py`（213 行）在位：`capture`/`diff` 子命令、token 用 `jwt_util.encode()` 现签（无硬编码口令）、`--ignore` 忽略运行时字段；只覆盖只读接口 + 写接口校验失败路径的设计声明合理 |
| P2-1 第一步 | `app.py` 实测 **2555 行**（与声称一致，原 3827）；`report_builders.py` 1368 行（声称 1369，差 1 行）；`db_handle.py` 单例句柄在位且注释说明循环依赖处理；`app.py:37-38` 导入块正确 |
| P2-5 下推 | `_place_event_counts()` 一次 GROUP BY（`count(id)` 口径与原实现一致，docstring 说明原因）；`_isolated_filter()` SQL 侧 NOT EXISTS（Event 覆盖事件-事件两侧 + 三张关系表）；`order_by(id.asc())` 显式化原 rowid 依赖 |
| 关系查询改写 | `model_search.py:537-545` 全图分支改为 `MATCH (n)-[r]-(m) RETURN r`，注释含 202 秒实测与语义等价说明 |
| 工作单回写 | R1-1 ~ R1-5 已补勾（上轮审核指出的遗漏已处理）；R2 人工项正确保持未勾 |
| 语法与测试 | `compileall`（3.8 解释器）通过；RAG 测试复跑 **349 passed**，与记录一致 |

### 2.2 采信记录（本机无法复现，方法论健全）

- **37 个请求 diff 完全一致**：需起两个实例打真实库。该工具确有驱动性——「diff 报 15 处 500（漏 import）→ 修复 → 复跑一致」的叙述自洽，这正是它优于打桩冒烟的证据（500 出现在打桩脚本不会调的接口上）。
- **性能数字**（path 写法 202s → 120s、限量分支 0.78s、质检 1.0s、参数化 vs 内联 473/369ms）：无法复测，但「全图加载模式本身不可用」的结论与首轮审查判断互相印证。
- **ruff F821 全量复查**：本机无 ruff / pyflakes，无法独立复跑；compileall + diff 一致可作间接佐证。

## 三、审核发现的问题

1. **提交数自列差一**：记录第一节说「新增 5 个提交」，实际 6 个——提交实施记录本身的 `fdf98eb` 未自列。与第 2 轮「三个提交」笔误同类，属固定盲区，建议以后实施记录末步固定加一行说明。
2. **ruff 门禁未固化**：F821 检查只在实施者本机跑过，建议进 CI 的 legacy-backend job，使该门禁不依赖某台机器。
3. **对照覆盖面**：37 个请求未覆盖 `limit` 边界、非 1 号节点、动态版本号（记录第七节已自认，列入下一轮工作单）。

## 四、遗留（人工，三轮持续未变）

- **R2-1 凭据轮换仍未执行**：`backend/.env` 实测仍含历史口令（grep 命中），git 历史中凭据在公开仓库仍可取回。全项目唯一安全敞口，不依赖任何代码。
- R2-2 完整端到端回归、feishu-bot 提交、RAG-1 部署时启用 `auth_basic`：均待人工。

下一轮的执行清单见 [修复工作单-第3轮-20260924收口.md](修复工作单-第3轮-20260924收口.md)。
