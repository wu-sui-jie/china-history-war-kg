# 修复审核报告（第 4 轮，2026-09-25）

> 审核对象：[修复实施记录-第4轮-20260925.md](修复实施记录-第4轮-20260925.md) 及其提交（`70c41b2` → `0b201f5` 等）。
> 审核依据：[修复工作单-第3轮-20260924收口.md](修复工作单-第3轮-20260924收口.md) 的 S3/S4 各项。
> 审核方式：对照源码逐项抽查、四门禁独立复跑、核对提交序列；「已复核」为本审核直接验证，「采信记录」为本机无法复现、采信实施记录自述。
> 本轮审核同时执行了工作单的 S1-1（凭据轮换）与 S2 的 API 级部分，见第四节。

## 一、审核结论

**通过。** 第 4 轮声称的 S3-1 ~ S3-8 共 12 项工作逐项核对代码、提交与测试，无一虚报；RAG 349 passed、前端 54 passed、compileall、ruff F821 四门禁独立复跑全部通过。三处「被工具抓出来的旧 bug」均有实证（ruff 抓到的 59 处未定义名已补、ASI 分号 bug 与悬空引用已被新测试先红再修）。上一轮审核提出的「实施记录自列提交差一」固定盲区已整改（`0b201f5` 提交信息明确含记录自身）。

## 二、逐项验证记录

### 2.1 已复核通过

| 项 | 验证结果 |
| --- | --- |
| S3-1 对照工具扩充 | `snapshot_responses.py` 328 行，`{node_id}`/`{version_id}` 占位符在位，`pageNum`/`pageSize` 参数名修正；`ci.yml:162-168` ruff==0.16.8 固化进 legacy-backend job，**本机复跑 All checks passed** |
| 59 处未定义名修复 | `import_json_to_sqlite.py` 的 `_safe_text`(41)/`_safe_float`(53)/`normalize_event_relation_type` import(19) 已补 |
| S3-2 拆 LLM 流水线 | `app.py` 实测 1572 行、`llm_pipeline.py` 1023 行、`eer_path.py` 22 行；`get_shared_extractors`(916) 单例在位；模块级死函数 `_extract_query_entities` 已删 |
| S3-3 N+1 下推 | `tools/profile_overviews.py`（172 行）存在；`_bulk_duplicate_counts`(603)/`_bulk_event_relation_counts`(608) 在 report_builders 并被调用 |
| S3-4 前端测试底座 | `tests/component` + `tests/unit` 在位，vitest 2.1.9 进 package.json，CI legacy-frontend 加 `test:component`；**实跑 `pnpm test`：54 passed（4 文件）** |
| S3-5 第一步 | `inference-render.ts` 436 行、页面 3007 行、渲染单测实数 16 例；`getGraphNodeDisplayName` 已定义进模块(66)并被引用(293) |
| S3-6 FE-4/FE-6 | `useNodeCrudPage.ts`（244 行）被四页引用；views/composables 下 `Http.` 引用实数 0；四页全部有 `lang="ts"`；`module/node.ts`、`module/graph.ts` 在位 |
| S3-7 全文索引评估 | `evaluate_fulltext_index.py`（269 行）默认只读、`--with-index` 才建索引、清理路径完整（DROP INDEX / DETACH DELETE）；结论「暂不上」附触发条件 |
| S3-8 编码治理 | `common_utils.repair_mojibake`(61)/`repair_mojibake_props`(82) 在位并接入两个写库出口；前端 `knowledge.ts` 乱码 key 已删（仅剩第 73 行说明性注释）；`knowledge.test.ts` 乱码透传断言在位 |
| S4-1 / S4-2 | feishu-bot 已提交（`3c53ee9`、`ac72406`）；实施记录自列提交的盲区已整改 |

### 2.2 采信记录（本机无法复现，方法论健全）

- 67 请求 diff 完全一致（需双实例 + 真实库）；性能数字（map 2.68s→0.13s / SQL 6313→17、timeline 2.16s→0.054s、全文索引耗时与命中集差异）；端口双监听（SO_REUSEADDR）坑的排查过程自洽。

## 三、本审核发现的问题（已在本次执行中处理）

1. **对账口径 bug（真实数据误报）**：`build_sync_reconciliation` 用行数对比，而 SQLite 关系表允许同名行、Neo4j 按 (标签, 名称) 唯一合并——实测 Place 行数差 2433 全部来自重名行（5316 行 / 2883 个 distinct 名称，与 Neo4j 节点数分毫不差）。**已修复**：口径改为 `COUNT(DISTINCT name)` 并在 docstring 记录实测依据；复验四类实体全部 `ok`、`consistent=True`、workbench `sync_mismatch=0`。
2. S3-5 第二步（模板拆子组件、1850 行样式外移、约 170 处 any）未做——记录已如实列入第十节，见收口工作单。
3. feishu-bot 工作区仍有并行会话产生的未提交文件（非本轮范围）。

## 四、本次审核顺带执行的事项（工作单 S1-1 / S2）

| 项 | 结果 |
| --- | --- |
| **S1-1 凭据轮换（四轮悬置的安全敞口，已闭环）** | Neo4j 口令已 `ALTER USER` 轮换（旧口令实测失效）；`JWT_SECRET` 已换新（token 带 `exp`/`iat`）；`backend/.env` 同步更新；后端模块用新凭据连接成功（7470 节点无损）；5000 端口无旧进程、无需重启。git 历史重写（filter-repo）仍为独立决策项 |
| **S2 API 级端到端回归（真实库，20/20 通过）** | 鉴权三态 401、身份不串号、注册建 viewer、viewer 写接口 403、创建→更新→删除的双侧同步（SQLite + Neo4j，含 neo4j_id 回写与删除清理）、对账接口、导入脚本可执行（无 NameError 阻塞）、孤儿演练（未发现孤儿）。浏览器 UI 点击清单另行人工执行 |
| S2 未覆盖部分 | 浏览器视觉/交互（弹层样式、SSE 逐字输出、iframe 渲染）仍需人工 |

## 五、当前剩余（更新后的全局状态）

- **S1-2**：部署时启用 `auth_basic` + 改 `CORS_ALLOW_ORIGINS`（配置就位，未启用）。
- **S2 浏览器部分**：人工点击回归。
- **S3-5 第二步**：模板拆子组件、样式外移、any 清理。
- **路由蓝图**：app.py 收官步骤（纯机械搬家）。
- **EER 收口**：recall 公式、缓存污染、metadata 模型名（见 [修复工作单-第3轮-20260924收口.md](修复工作单-第3轮-20260924收口.md) 备忘节）。
- **git filter-repo 历史重写**：决策项。
- feishu-bot 未提交文件（并行会话自管理）。
