# 修复工作单（第 10 轮，2026-09-25）

> 依据：[修复实施记录-第9轮](修复实施记录-第9轮-20260925.md) 的验收结论（见[项目现状与后续计划](项目现状与后续计划.md)第一阶段），
> 以及**用户已明确授权**的事项（backup.cmd 删除）。按 W1 → W2 → W3 → W4 顺序执行。

## W1 删除 backup.cmd（用户已授权，10 分钟内做完）

**干什么**：删除仓库根的 `backup.cmd`（DOC-4 闭环）。

**为什么安全（第 10 轮审核已确认）**：全仓库扫描（*.py / *.md / *.yml / *.cmd / *.json），
引用它的**只有 4 处文档描述**（实施记录与现状计划），**没有任何代码、脚本或 CI 步骤引用**——
它是一次性备份脚本（指向私人代理端口 65532 与私人仓库 `whatthefact/debug-debug-backup`），
对项目功能零影响。

**怎么干**：直接删除本地文件 `backup.cmd`（git 侧在 P1-8 已移出跟踪、`.gitignore:35` 已有
`/backup.cmd`，无 git 操作）。**同时**勾选[项目审查与修复清单](项目审查与修复清单.md) DOC-4、
更新[项目现状与后续计划](项目现状与后续计划.md)阶段 0-1 为已完成。

**验收**：`ls backup.cmd` 报不存在；`git status` 干净；清单 DOC-4 状态改为「已修复」。

**边界说明**（如实）：该文件历史曾在公开仓库（初始提交起可取回），删除只影响当前与未来，
不撤回历史——彻底清除仍走 `git filter-repo` 决策项。

## W2 盘符路径清理（6 处，位置已核，约 15 分钟）

**干什么**：文档正文不再出现内联解释器路径，统一 `conda activate` 口径。

**怎么干**（6 处位置全部核过）：

| 位置 | 现状 | 改为 |
| --- | --- | --- |
| `README.md:168` | 环境表「本机对应环境（`E:/anaconda`）」 | 列名改「conda 环境名」，填 `place-name-KG` / `AI_Agent`，指向文末「本机环境备注」 |
| `README.md:193` | `E:/anaconda/envs/AI_Agent/python.exe scripts/run_server.py ...` | `conda activate AI_Agent && python scripts/run_server.py --port 8000 --version 20260915_v1` |
| `README.md:204-205` | 两条 `E:/anaconda/envs/place-name-KG/python.exe -m pip install ...` | `conda activate place-name-KG && python -m pip install -r requirements.txt` / `-e entity-event-relation` |
| `README.md:208` | `E:/anaconda/envs/place-name-KG/python.exe app.py` | `conda activate place-name-KG && python app.py` |
| `backend/README.md:223` | 同旧后端启动 | 同上 |

**验收**：`grep -rn "E:/anaconda" README.md backend/README.md docs/` 零命中；
「本机环境备注」章节保留（那里是盘符路径的唯一合法位置）。

## W3 EER 阶段 2（工程债分批，按依赖顺序）

第 9 轮已收口 EER-15/4/11（原子写这一半）。剩余按此顺序：

- **W3-1 EER-12 geocoding 健壮化**：`war_extraction/geocoding/geocode_amap.py` 单次失败改
  3 次指数退避（区分偶发网络错误与配额耗尽 infocode 10003/10044 并分别提示）；
  逐条落盘替代批次末尾一次性保存（中途失败不整批重来）。
  验收：注入超时/配额错误各一条用例，断言重试次数与部分结果保留。
- **W3-2 EER-7 剩余：抽取编排单一事实源**——backend 的 `blueprints/llm.py` + `llm_pipeline`
  与 EER `main.py` 是两份平行编排。把编排抽进 war_extraction 包（对外一个入口函数，
  backend 调它，EER main.py 也调它）。验收：两边的抽取结果结构完全一致（同一输入 diff），
  backend 32 例 pytest 全绿。
- **W3-3 EER-6 重复收敛**：四份 `_extract_json_payload`、两份 `_split_multi_value`、
  两份 `_parse_year_for_order`、两份关系仲裁合并到公共模块。验收：纯重构，
  EER 21 例 + backend 32 例全绿。
- **W3-4 EER-10 评估器二选一**：保留 `optimal_evaluator`，归档/删除 `evaluator.py` 与
  `metrics_calculator.py` 的死代码部分。验收：`evaluate.py` 输出不变，grep 无残留引用。

**geocoding 硬编码 `../../backend/database` 读写主库的问题**随 W3-2 一起改（改为配置参数，
默认指向 `backend/database` 但可被环境变量覆盖）。

## W4 快照取舍（决策，不写代码）

`evaluation/latest`（2026-04-20 旧口径）与 `evaluation/recheck-micro-20260925`（修前产物）
都是**修前**快照。第 9 轮修后定值：关系 743/145/356、F1=0.747861。二选一：
- **保留作历史基线**（推荐）：在两个快照的 metadata 加注"修前口径，重跑数值见第 9 轮记录"；
- 或用新口径重跑覆盖（历史对比链路丢失，不推荐）。

## W5 提交纪律（每轮固定动作）

第 9 轮的 11 个文件改动目前仍在工作区（与第 6 轮同款问题）。本轮起按第 4 轮定下的约定收尾：
跑验证 → 勾选工作单 → 写实施记录 → **实施记录与代码同一批提交（含记录本身）**。

## 完成后的下一步（预览）

W1-W4 收口后，代码侧遗留只剩「inference 页模板拆分（已到 403 行，剩余为渐进）」与
「部署期动作（RAG-1/RAG-8）」；三个决策项（filter-repo / 隔离方案 B / 角色版本号）触发条件
均未满足，可继续挂起。届时项目可认为进入维护态。
