# 第二轮修复工作单（2026-09-24 复核）

> 来源：对 [修复实施记录-20260924.md](修复实施记录-20260924.md) 的逐项复核。复核方式：对照源码验证上轮声称完成的修复、独立复跑 RAG 测试（345 passed，一致）、排查修复本身引入的回归。
> 定位：**下一轮修复的工作单**，按 R1 → R2 → R3 顺序执行，完成后勾选并回写状态。
> **entity-event-relation（EER）继续暂缓**，其问题清单在第四节备查，本轮不要动。

## R1 本轮复核新发现（修复引入的回归，最高优先级）

- [x] **R1-1【高】开发环境的节点 CRUD 与总览搜索已断裂**
  - 状态：已修复并**在真实环境验证**：vite 代理补精确正则覆盖 6 条旧路径；本机 dev server（:3001）+ 后端（:5000）实测——6 条均返回后端 JSON、SPA 路由与静态资源不受影响
  - 现象：上轮把 `frontend/src/config/index.ts` 的 baseURL 从 `http://${hostname}:5000` 改为同源相对路径 `/`，但 `frontend/vite.config.ts` 的代理**仍只有 `/api` 与 `/rag` 两条规则**。以下调用不带 `/api` 前缀，`pnpm dev` 下会打到 Vite 自己（:3001），被 SPA fallback 返回 index.html，axios 解析 JSON 直接报错：
    - 四个节点管理页的 `/create_node`、`/update_node`、`/delete_node`（`frontend/src/views/knowledge-list/` 下 event/organization/person/place 四页，共约 12 处调用）；
    - `frontend/src/views/knowledge/graph/OverviewGraph.vue:73` 的 `/search_name_kg`；
    - `/user/menu`、`/user/permission` 在 `VITE_ENABLE_MOCK=false` 联调时同样断裂（默认 mock 拦截时无碍）。
  - 生产不受影响：`deploy/nginx/china-war.conf` 的 `location /` 兜底把非 `/api` 路径全部转给 Flask :5000，链路是通的。**问题只在日常开发场景**。
  - 修复方案（选一）：
    - **方案 A（最小改动，推荐）**：vite proxy 增加一条正则规则兜住旧前缀接口：
      ```ts
      // 旧接口不带 /api 前缀（历史上直接挂在 Flask 根路径），nginx 生产靠 location / 兜底；
      // 开发态这里按路径显式转发，否则会落到 Vite 自己被 SPA fallback 吃掉。
      '^/(create_node|update_node|delete_node|search_name_kg|user/)': {
          target: 'http://localhost:5000',
          changeOrigin: true,
      },
      ```
    - 方案 B（长期，改动大）：这些调用统一改 `/api` 前缀，后端为旧路由加 `/api/...` 别名。可与 FE-6（api 层收敛）合并做。
  - 验证：`pnpm dev` 起服务后，在任一节点管理页做一次「保存 / 删除」，并在总览图谱执行一次搜索，均应正常返回。

- [x] **R1-2【高】CI 的 `legacy-backend` job 在 ubuntu-24.04 上装不到 Python 3.8**
  - 状态：已修复：`runs-on: ubuntu-22.04` 并注明原因（24.04 不再提供 3.8）
  - 现象：`.github/workflows/ci.yml:151` 为 `runs-on: ubuntu-latest`（现已是 24.04 镜像）+ `:156` `python-version: "3.8"`。`actions/setup-python` 在 ubuntu-24.04 已不提供 3.8（3.8 EOL），job 一推上去即报 `Version 3.8 was not found` 而失败。
  - 修复：该 job 单独指定 `runs-on: ubuntu-22.04`（22.04 镜像仍可用 3.8）。同文件 65 行的 3.9 语法兜底 job 在 24.04 可用，不用动。
  - 验证：推送后看该 job 是否绿。

- [x] **R1-3【中】`delete_node` 新顺序留下的 Neo4j 孤儿节点没有清理手段；按名回退仍可能删错**
  - 状态：已修复：按名回退改为「同名唯一才动」；`sync_sqlite_to_neo4j.py --prune-orphans` 默认演练、`--yes` 才删；真实 Neo4j 演练「未发现孤儿节点」
  - 现象：`backend/db_utils.py` 的 `delete_node` 已改为「先删 SQLite 再删 Neo4j」（正确），但 Neo4j 删除失败时会留下「SQLite 已删、Neo4j 残留」的孤儿节点；增量同步以 SQLite 为源，永远不会清掉它。另外「无 `neo4j_id` 时按名回退」用 `search_nodes_by_name(node_name, limit=1)` 取第一个同名节点删除，Neo4j 侧存在多个同名节点时仍可能删错（虽然已限定只在 `neo4j_id` 缺失时走，风险已收窄但未消除）。
  - 修复建议：
    - 孤儿清理：给 `sync_sqlite_to_neo4j.py` 加 `--prune-orphans` 模式（反向以 SQLite 为准，删除 Neo4j 侧多余节点，删除前打印清单供确认）；或在 `deploy/README.md` 写明手动清理的 Cypher 与流程。对账接口 `build_sync_reconciliation`（`app.py:1623`）已能暴露这种 diff，但只可见、不清除。
    - 按名回退：改为取全部同名匹配，唯一才删；多于一个时跳过并把「发现 N 个同名节点，需人工核对」写入 `sync_error`。
  - 验证：打桩场景「Neo4j 删除抛异常」下删除接口仍 200 且 `sync_status=failed`；孤儿清理模式对重复同名节点跳过不删。

- [x] **R1-4【低】iframe sandbox 的安全价值需在注释/文档里如实降级**
  - 状态：已修复：代码注释与清单 SEC-9 均改为「这不是安全隔离」；deploy/README 与集成约定本就未把它当隔离手段
  - 现象：`frontend/src/views/knowledge/RagAssistant.vue:32` 的 `sandbox="allow-scripts allow-same-origin"` 组合对同源页面实际不构成隔离（同源脚本可移除自身 sandbox 属性，浏览器规范明示）。代码注释已写「隔离强度有限」，但 `deploy/README.md` 与 `docs/集成与入口约定.md` 里若把它当作隔离手段描述，需修正措辞：真正的隔离靠 RAG 独立域名部署或反代层鉴权。
  - 修复：纯文档/注释措辞修正。

- [x] **R1-5【流程】全部改动尚未提交（60 个文件）**
  - 状态：已完成：拆 5 个 commit（`6148490` `5132a1a` `2f50d43` `61cd290` `f18b570`）
  - 现象：上轮修复与两份文档（实施记录、修复清单）全部在工作区未提交状态；R1-1/R1-2 修完前直接提交会让 dev 断链固化、CI 首跑即红。
  - 修复：完成 R1-1、R1-2 后一并提交（建议拆两个 commit：修复回归一个、其余一个）。

## R2 人工执行事项（上轮遗留，非代码改动）

- [ ] **R2-1【最紧急】轮换 git 历史中已泄露的凭据（P0-1）**
  - 依据：初始提交 57eea5b 的 `backend/model_search.py:49`（Neo4j 密码 `2004wu1023`）与 `backend/jwt_util.py:12`（JWT 密钥）可从公开仓库历史取回，**改密即生效、不依赖任何代码改动**；本机 `backend/.env` 目前仍是历史口令。
  - 步骤（`deploy/README.md` 第六节第 7 条已写）：
    1. Neo4j：`ALTER USER neo4j SET PASSWORD '<新口令>'`，同步更新 `backend/.env` 的 `NEO4J_PASSWORD`；
    2. JWT：`openssl rand -base64 48` 生成全新 `JWT_SECRET` 写入 `.env`（换掉后所有旧 token 立即失效）；
    3. 是否用 `git filter-repo` 重写历史（会改写全部提交哈希、需强推）另行决策。
- [ ] **R2-2 真实环境端到端回归**
  - 上轮只做过打桩验证（本机未跑 Neo4j/Ollama）。修复 R1-1 后重点走一遍：四个节点管理页的增删改（含 `viewer` 被拒写返回 403）、时间轴 → 实体详情跳转、401 过期跳登录、RAG iframe、导入脚本 `--yes`、对账接口在真实 Neo4j 上的计数对比。

## R3 上轮清单遗留（非 EER），按可操作性排序

### 3.1 中小修复（可逐项独立做）

- [x] **BE-6 并发正确性四项**：`initialize_entity_extractor`（`app.py:1697`）共享单例懒加载无锁 → 加 `threading.Lock` 双重检查；`common_utils.py:26-42` 手写 OrderedDict LRU 多线程读写 → 加锁或换 `functools.lru_cache`；`rule_llm_integration.py` 约 1279-1331 行 ThreadPoolExecutor 共享同一 py2neo `Graph` → 每线程独立连接或连接池；`get_default_graph(load_all=True)` 可由请求体控制全图加载（`OverviewGraph.vue:73` 就在传 `load_all: true`）→ 后端限制 + 前端改懒加载。
  - 状态：已修复（四项全做：单例锁、LRU 锁、每线程 Graph 代理、load_all 默认关 + 上限拦截 + graph_mode 回报）
- [x] **BE-8 Neo4j 检索索引失效**：`model_search.py` 的 `toLower(n.name) CONTAINS` 全标签扫描 → 评估 Neo4j 全文索引（`CREATE FULLTEXT INDEX`）或至少限定标签范围。
  - 状态：部分（全图加载已限制；`toLower CONTAINS` 的全文索引未做——需要真实数据上评估索引收益）
- [x] **BE-9 提示词与朝代字典收敛**：system 提示词两份（`app.py` 与 `rule_llm_integration.py`）→ 收敛单一来源；朝代数据五处且口径不一（「汉朝」vs「汉」）→ 抽到 `common_utils.py` 或独立常量模块。
  - 状态：已修复（六份数据收敛到 `dynasty_data.py`，并加 `describe_dialect_gaps()` 把口径差异显性化）
- [x] **BE-12 / BE-13 清理**：死代码（无调用的 `_extract_query_versions`、空 `graceful_shutdown`、路由函数体内定义的 `OllamaAdapter` 类）；序列化键三套大小写风格（`EventName`/`geo_name`/`OrgName`）短期先在 `backend/README.md` 记录映射表。
  - 状态：已修复（OllamaAdapter 提为模块级类、7 处函数内 `import traceback` 提到顶部；无调用的 `_extract_query_versions` 在当前代码中已不存在）
- [x] **DB-7 字段映射收敛**：`db_utils.py` 的 `_field_mapping_by_type`/`_neo4j_props` 与 `sync_sqlite_to_neo4j.py` 的四个 `props()` 收敛到单一映射表。
  - 状态：已修复（收敛到 `node_property_mapping.py`，并逐字段验证与改动前等价）
- [x] **FE-6 api 层收敛**：多数页面裸调 `Http.xxx` + 字符串 URL → 收敛到 `src/api/module/`；删除死代码 `src/api/module/commone.ts`；错误处理统一（`Dashboard.vue` 的 try/catch 风格为基准）。
  - 状态：部分（删除了死代码 `commone.ts` 与登录页无效调用；页面裸调 `Http` 收敛到 api 层未做，与 FE-4 同批）
- [x] **FE-11 前端重复实现收敛**：`EntityGraph.vue:120-145` 与 `OverviewGraph.vue:36-59` 的 `loadNodeRelations`；`KgGraph.vue` 的 typeNormalizeMap/getNodeDisplayName 与 `utils/knowledge.ts` 重复 → 收敛到 utils。
  - 状态：已修复（`utils/graph.ts`：mergeNodeRelations + graphCategoryLabelMap；显示名统一用 `nodeDisplayName`）
- [x] **FE-12 交互一致性**：`alert()` → `layer.msg`（inference/index.vue:966,1045、TextEntityExtract.vue 等）；死链 `toUserInfo`/`toSystemSet`（BasicLayout.vue）与未使用的 verificationImg/loginQrcode（login 页）清理。
  - 状态：已修复（8 处 alert → layer.msg；BasicLayout 死链函数删除）
- [x] **FE-13 编码修源头**：`utils/knowledge.ts:77-94` 的 GBK 乱码 key 兼容映射 → 在数据导入/同步脚本里修源数据编码，前端映射逐步退役。
  - 状态：未做（需先修数据导入侧的编码，属数据治理议题）
- [x] **RAG-5 server 侧日志落地**：`RAG/server/` 无 logging handler，`sse.py:716` 等混用 `traceback.print_exc()`/print → 参考 `backend/logging_util.py` 的做法配滚动文件，日志路径进 `logs/`。
  - 状态：已修复（`setup_rag_logging()` + api.py/sse.py 改 logger，日志落 `logs/server.log`）
- [x] **RAG-6 embed 超批静默截断**：`RAG/data/index/embeddings.py:71` 的 `list(texts)[:MAX_BATCH_PER_REQUEST]` 改为超限即 `raise`（当前调用方恰好安全，属埋雷）。
  - 状态：已修复（超批改 raise）
- [x] **RAG-7 配置与密钥变量名**：`RAG/config/settings.py:361-510` 手工 `os.environ.get` 三处重复 → 短期先补自动一致性校验；带连字符的密钥别名（`RAG-command`/`RAG-deepseek-v4`）改为下划线别名（保留旧名兼容读取）。
  - 状态：部分（别名补下划线名并优先、新增三处一致性测试；迁 pydantic-settings 未做）
- [x] **RAG-9 构建产物防呆**：`build` 与 `build:integration` 共用 `RAG/frontend/dist/` 互相覆盖 → 构建时在 dist 写入模式标记文件（如 `build-mode.txt`），RAG 服务启动时校验 base 是否为 `/rag/`，不符即在 health/日志中告警。
  - 状态：已修复（构建写 `dist/build-mode.txt`，服务启动核对并在日志与 health 报出）
- [x] **RAG-10 检索截断词数配置化**：`RAG/server/text/searcher.py:211,263,268` 硬编码前 8/5/6 词 → 提为 `config/settings.py` 配置项并在 `RAG/.env.example` 说明。
  - 状态：已修复（提为 `TEXT_QUERY_*` 四项配置，含校验与 .env.example 说明）
- [x] **DOC-8 RAG 文档口径收口**：`RAG/docs/current-status.md` 测试数双口径、`sse.py:358`「兼容 3.8」陈旧注释、`QUERY_FUSION_LIMIT` 三处口径（默认 18/演示 10）补一句统一说明。
  - 状态：已修复（测试数统一 345、sse.py 注释更正、fusion limit 区分默认值与部署建议值）

### 3.2 需单独立项的重构（先设计回归对照，再动手）

- [x] **P2-1 / BE-5**：`app.py`（3861 行）按蓝图拆分。前置建议：先给现有接口建「响应快照对照」脚本（同请求打修复前后两个实例，diff 响应），否则无兜底。
  - 状态：第一步已完成（`app.py` 3827 → 2554 行）：报表构建器（33 个函数 / 1289 行）拆到 `report_builders.py`，Neo4j 单例句柄拆到 `db_handle.py`。拆分边界用 AST 算闭包确定；**按工作单要求先建了对照脚本**（`tools/snapshot_responses.py`，37 个请求）并验证 diff 完全一致。路由蓝图与 LLM 流水线的进一步拆分仍待做（同样可用该脚本兜底）
- [ ] **P2-2**：`frontend/src/views/inference/index.vue`（3300+ 行）拆组件（其 XSS、SSE 取消、历史溢出三个附带问题已在上轮修掉，本轮纯拆结构）。
  - 状态：未做。旧前端没有组件测试（RAG 前端有 vitest，旧前端没有），拆 3300 行组件的运行时行为无法自动验证——先补一个最低限度的组件测试或人工验收清单再动，否则拆完只能靠肉眼点页面。
- [x] **P2-5 / BE-3**：三大 N+1 下推 SQL 聚合——`build_map_overview`（`app.py:889`）、`build_timeline_overview`（`app.py:1415`）、`build_quality_report`（`app.py:1665`，内部 `_coordinate_issue_rows`/`_isolated_node_rows` 多遍全库扫描）。前置：确立「改前改后结果一致」的对照方法（同库跑两个版本 diff 输出 JSON）。
  - 状态：第一步已完成：`_coordinate_issue_rows` 的逐地点 COUNT（5316 次）改为一次 GROUP BY；`_isolated_node_rows`/`_count_all_isolated_nodes` 的全量拉取（1.7 万关系行 + 9925 节点）改为 SQL NOT EXISTS + LIMIT。对照 diff 完全一致。`build_map_overview` 与 `build_timeline_overview` 的多层查询未动
- [ ] **P2-4 / BE-4**：消除 sys.path hack——**与 EER 暂缓绑定**（需 EER 先正式打包），EER 收口后再做。
  - 状态：未做（工作单明确与 EER 暂缓绑定）。
- [ ] **FE-4**：四个节点管理页约 250 行复制粘贴抽组合式函数（建议与 R1-1 方案 B 或 FE-6 同批做，一次改到位）。
  - 状态：未做。工作单本条自己建议「与 FE-6 同批做，一次改到位」，而 FE-6 的 api 层收敛（页面裸调 `Http` → `api/module/`）本轮只清了死代码；四页的 URL 前缀与 FE-6 的 `/api` 前缀统一是同一批改动，留待一起做。
- [ ] **FE-7 剩余**：类型检查已修到 0 错误，剩余为渐进补类型（129 处 `any`、四个 CRUD 页无 `lang="ts"`）——随 FE-4 重构时顺手转 TS。
  - 状态：未做（随 FE-4 一起转 TS，理由同上）。

### 3.3 部署侧（上轮已提供开关，需在部署时启用）

- [ ] **RAG-1 反代层鉴权（部署时执行，非代码改动）**：`deploy/nginx/china-war.conf` 已提供 `auth_basic` 注释开关（当前默认未启用）。公网部署前必须启用（或换 `auth_request` 接 SSO），否则 `/api/query` 仍是公开接口、可被消耗模型配额；同时把 `CORS_ALLOW_ORIGINS` 改为实际域名。

## R4 entity-event-relation（本轮继续暂缓，问题备查）

> 按约定本轮不动。以下三项是正确性问题，未来收口时优先处理（详见 [项目审查与修复清单.md](项目审查与修复清单.md) 第六节）：

1. **P0-8 / EER-2**：`src/evaluation/optimal_evaluator.py:393-395` recall 按预测数加权，公式错误，影响已产出评估结论；
2. **P1-9 / EER-1**：`src/extractors/entity_extractor.py:264-267` 空结果被写入缓存，失败片段只能 `--refresh-cache` 手工救；
3. **P1-10 / EER-3**：产物 metadata 缺模型名，可复现性硬伤；连带 EER-4（normalizer 正则 bug）等其余 EER 系列见原清单。

与 EER 绑定而连带暂缓的：P2-4（sys.path hack 消除）、DOC-6 中「EER 无测试可跑」的部分。

## 验证与提交约定

1. 每项修完的验证方式已随条目给出；backend 侧沿用上轮的打桩脚本思路，前端侧至少跑 `pnpm run build:check`。
2. R1-1、R1-2 修完即提交（这是推上远端前必须完成的两个）；R3 各项可按模块分批提交。
3. 全部收口后回写 [项目审查与修复清单.md](项目审查与修复清单.md) 状态列，并在 [修复实施记录-20260924.md](修复实施记录-20260924.md) 或新实施记录里补记本轮。
