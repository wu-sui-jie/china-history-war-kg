# 项目现状与后续计划（2026-09-25，第 13 轮复核整改第二批之后）

> **这份文档给谁看**：接手的人、复盘的人、决定下一步先做什么的人。一页讲清三件事——
> **已经做完的**（按主题）、**还没做的**（按性质分类，写清为什么没做、什么条件下做）、
> **接下来打算怎么做**（顺序 + 验收标准）。
>
> **不重复维护明细**（项目自己的文档约定，见 [README.md](README.md) 的「文档维护约定」）：
> 逐条问题的状态在 [项目审查与修复清单.md](项目审查与修复清单.md)；
> 每一轮改了什么、怎么验证的，在 `修复实施记录-第 N 轮-*.md`；
> RAG 的版本号、测试数、数据计数只在 [RAG/docs/current-status.md](../RAG/docs/current-status.md)。
> 本文只做汇总、分类与排序。
>
> **本版更新**：补上第 12、13 轮与两批"第 13 轮复核整改"的结果（此前本文件停在第十一轮）。
> 再审查清单的第一批 8 项直接缺陷已全部修掉（口令策略统一、前端不再 trim 口令、限流计数原子化、
> 限流故障 fail-closed、安装门禁拒绝占位密钥、SSE 先鉴权、撤销策略显式选择、systemd 依赖）。
> 第 13 轮把飞书队列、节点 outbox、RAG 鉴权分档、登录限流做完后，自己声明了 14 条
> "不能算整体完成"的理由；本轮（[第 13 轮复核整改](修复实施记录-第13轮复核整改-20260925.md)）
> 把其中**属于代码缺陷、且不依赖服务器**的四条修掉了：登录限流落库时间、RAG 凭证撤销、
> nginx 档的两份配置不一致、旧后端 API 错误格式。
> **剩下的全部是"部署时动作 / 产品决策 / Python 版本统一"三类**，没有别的代码缺陷项。
> 第五节数字为 2026-09-25 本轮实跑值。

## 一、一句话现状

四个模块（旧后端 `backend/`、旧前端 `frontend/`、RAG 问答、离线抽取 `entity-event-relation/`）
经过**十三轮迭代与多轮独立审核**（第 8 轮是复核轮，最新为第 13 轮复核整改）之后，
功能、安全基线与自动化门禁都已闭环。**代码侧的缺陷项已经清空**：第 13 轮列出的 14 条"不能算完成"的理由、以及再审查清单
新发现的 15 个问题中属于"直接缺陷"的 8 项，都已修掉并各有常驻用例；
剩下的都不是"写代码就能解决"的——

| 剩余类别 | 具体是什么 | 触发条件 |
| --- | --- | --- |
| 部署时动作 | 真实 nginx / HTTPS / systemd / outbox timer / 两侧密钥配置 / 飞书与 LLM 真实链路 | 买服务器后（复核清单第十三节；`deploy/scripts/check_rag_auth.sh` 会在安装时自动校验） |
| 产品决策 | 关系在线 CRUD（连带关系 outbox）、服务端问答历史、飞书分片 worker | 用户定方向后 |
| 前端功能开发 | 用户管理页的启用/停用/解锁/重置密码/强制下线 + 安全审计日志（再审查清单第二批） | 需要先确认 2.11 里那五项界面设计 |
| 认证架构变更 | JWT 不再长期放 localStorage（短期 access token + 刷新，或 HttpOnly Cookie + CSRF，再审查清单 §2.6） | 会牵动前端全部请求路径，需独立立项 |
| Python 版本统一 | 3.11 全量环境、SQLAlchemy 2 兼容、py2neo 替换、统一 lock | 需要新建环境 + 真实安装 + 四套测试在 3.11 全绿；**锁文件必须等全量验证通过后再生成** |

**当前唯一被阻塞的技术动作是「一次整本重跑」**：第 11 轮把提示词版本改成按提示词源码哈希机械派生
（`prompt-v2-20260420+4f975592`），派生值与旧串不同，`cache/` 里现有 **196 条分段缓存**
（索引与文件均实测 196 条，其 `context.prompt_version` 全是旧值 `prompt-v2-20260420`）在下次运行时**会全部未命中**，
必须真实调用大模型（196 段 × 3 阶段，DeepSeek 计费，耗时长）。这个动作同时能回答两件悬着的事——
第 10 轮那个提示词静默降级 bug 修复后的实际效果（第 11 轮 A-5），以及占位词排除集要不要统一为宽口径（A-6 第 1 条）。
缓存自动失效正是该改动的设计意图（改一个字符就换版本、不依赖人记得 bump），但**何时花这笔钱需要你决定**。
**本版记录的决定：先把工程完善，重跑往后放。**

> **⚠️ 评估口径已在第 11 轮变更，历史数值不可直接比较。**
> 关系抽取的五个规范事件-事件关系类型原先只做模糊比对，而它们**两两 `fuzz.ratio` 都是 50**
> （都带"关系"二字、4 字中 2 字），全部越过阈值 40——于是**类型判错也照样拿满分**。
> 改为「两侧都在规范集合内时要求精确相等」之后，同一份产物（指纹 `885b0d9ec340a50b`）：
>
> | 指标 | 改前 | 改后 |
> | --- | --- | --- |
> | 关系 tp/fp/fn | 743/145/356 | **714/174/385** |
> | 关系 F1 | 0.747861 | **0.718671** |
> | 宏观平均 F1 | 0.4565 | 0.4468 |
>
> 丢掉的 29 个 tp 是 **30 条「类型判错但靠模糊相似度 50 顶替另一种规范类型」的预测三元组**
> （29 条顺承关系 + 1 条条件关系），即改前的关系 F1 里约 4% 是白送的。**这是口径变更而非模型退化**；
> 引用历史记录里的 0.7479 时必须注明它属于旧口径。阈值敏感性表（四阈值 OAT、极差 0.1176/0.0538/0.0056/0.0070）
> 与结论见[第 11 轮记录第六节 C-6](修复实施记录-第11轮-20260925.md)。

> **一条反复付学费的教训（第 8、9 轮）**：验证"可复现"必须**显式固定多颗 `PYTHONHASHSEED` 做子进程比对**——
> 只跑两次说明不了任何事，两次完全可能碰巧落在同一个匹配结果上（第 8 轮据此把 EER-15 误判为"只影响错误样例、
> 不影响指标"，第 9 轮换种子复测才发现指标级漂移真实存在：关系 tp 739/743/741、F1 0.7438/0.7479/0.7458）。
> 完整过程见清单 EER-15 与[第 9 轮记录](修复实施记录-第9轮-20260925.md)。

## 二、已完成（按主题）

| 主题 | 做完了什么 | 证据/入口 |
| --- | --- | --- |
| 安全基线 | 泄露在 git 历史里的 Neo4j 口令与 JWT 密钥**已实际轮换**（旧口令实测失效）；鉴权三态（无 token / 伪造 / 过期）统一 HTTP 401；Cypher 全面参数化（标签用白名单校验）；inference 页 XSS 组合治理（DOMPurify）；CORS 白名单与 `debug`/`0.0.0.0` 收敛 | 清单 SEC-1~10；[第 5 轮记录](修复实施记录-第5轮-20260925.md)第一节 |
| 账户与权限闭环 | 角色模型 `admin ⊃ editor ⊃ viewer` 三层口径一致（后端接口 403 / 菜单裁剪 / 路由 meta）；用户管理页把提权界面化（不能改自己、角色白名单、实时生效）；菜单三级裁剪与提权链路有**常驻用例** | 清单 SEC-7；`backend/README.md` 的角色职责表；`backend/tests/`（**43 例**含菜单裁剪与提权链路）；[第 6 轮记录](修复实施记录-第6轮-20260925.md) |
| 问答记录按账号隔离 | 主应用 iframe 以 `cw-user` postMessage 下发 uid，RAG 按 uid 分桶；两条边界泄漏路径已堵（前端登出泄桶、RAG legacy 收编）；换账号作废排队轮次；uid 形状白名单 | 清单「问答记录按账号隔离」；`frontend/tests/component/inference-scope.test.ts`、`RAG/frontend/tests/e2e/user-scope-isolation.spec.ts` |
| 旧前端工程债 | 四个 CRUD 页抽公共组合式函数（1380 → 823 行）；问答页 **1189 → 403 行**（第 7 轮拆出 4 个子组件 SessionSidebar/ChatMessages/ChatInput/KgNodeDrawer，另有第 2 轮既有的 KgGraph 一并移入 components/；2 个新组合式函数 + 3 个 utils/api 模块）；样式改命名空间生效；类型检查进 CI；mockjs 从生产包移除 | [第 7 轮记录](修复实施记录-第7轮-20260925.md)第二节；`frontend/README.md` |
| 旧后端工程债 | `app.py` **1711 → 415 行**：报表构建器、LLM 流水线、路由蓝图三步拆完；角色常量与鉴权装饰器独立成 `roles.py`；N+1 三处下推（地图 2.68s→0.13s、时间轴 2.16s→0.054s）；200+ 处 print 换 logging | [第 7 轮记录](修复实施记录-第7轮-20260925.md)第三节；`backend/README.md` |
| 抽取链（EER）正确性与打包 | 三项正确性修复（recall 公式、空结果不落缓存、产物补模型名）；**正式打包**为 `war_extraction` 并删除 `sys.path` 注入 | [第 5 轮记录](修复实施记录-第5轮-20260925.md)第四节；[第 7 轮记录](修复实施记录-第7轮-20260925.md)第五节 |
| 评估可复现 + 抽取链小修（第 9 轮） | 评估指标不再随进程漂移（EER-15 定序化）；三条失效的地名噪声正则修好（EER-4）；缓存索引与结果文件改原子写、悬挂/损坏条目自动摘除（EER-11 原子写这一半）。抽取链**首次有自己的常驻用例**并进 CI | 清单 EER-4/11/15；`entity-event-relation/tests/`；[第 9 轮记录](修复实施记录-第9轮-20260925.md) |
| EER 工程债第一批（第 10 轮） | geocoding 健壮化（瞬时/配额/确定性三类分开、指数退避、逐条落盘，EER-12）；两套重复评估器删掉（EER-10）；四类重复逻辑收进公共模块（EER-6）；**查出并修掉 backend 抽取链一个静默降级 bug**（实体对象被当字符串塞进提示词，EER-7）；主库路径可配置（`EER_DB_PATH`）；`backup.cmd` 删除（DOC-4）；6 处盘符路径清理（DOC-5） | 清单 EER-6/7/10/12、DOC-4/5；[第 10 轮记录](修复实施记录-第10轮-20260925.md) |
| **编排合并 + 评估口径变更（第 11 轮）** | **两份抽取编排合成单一入口**：新增 `war_extraction/core/extraction_runner.py`（分段循环 + 三阶段调用 + 失败诊断只有这一份），backend 与离线链路各自的后处理改为钩子挂入，两侧都不许再自建阶段循环（AST 断言钉住）；关系类型改**精确相等**（口径变更，见第一节）；**路径全面按 `__file__` 锚定** + 缓存孤儿 GC（TTL 只报告不自动删，因为这份缓存同时是产物的可复现路径）；backend 抽取端点补**录制回放**常驻门禁（三层防护的分工写进 `snapshot_responses.py` 注释）；`PROMPT_VERSION` 改按源码哈希机械派生；geocoding 进度文件改一批一个；日配额截断不再"悄悄跑完剩下的流程"；新增 `data/annotations/README.md` 标注规范 | 清单 EER-7/9/11/13/14、DOC-6；`entity-event-relation/tests/`（**104 例**）、`backend/tests/`（**43 例**）；[第 11 轮记录](修复实施记录-第11轮-20260925.md) |
| 自动化门禁 | CI 覆盖四个模块；六套测试全绿（数字见第五节）；接口行为不变由 **67 请求快照**对照工具兜底；提权/隔离等新代码有常驻用例与浏览器级验收 | `.github/workflows/ci.yml`；`backend/tools/snapshot_responses.py` |
| **三处决策项收口（2026-09-25）** | 删掉一律丢弃人名 `{秦始皇, 吴起}` 的过滤名单（实测两者是标注里的 gold 却被丢，而关系里作「统帅」「君主」各 2 条）；占位词排除集的宽/窄分歧**统一为宽口径**、窄集删除；删掉零引用的孤儿模块 `utils/alignment.py`。三项都有常驻用例（`tests/test_decision_filters.py`，先确认改前失败），并实测证明**当前产物的评估结果不受影响**（改前/改后各跑一次评估，除时间戳外逐字段相同） | [决策项收口记录](修复实施记录-决策项收口-20260925.md) |
| 文档体系 | 两级索引无断链；RAG 的版本/计数有唯一事实源并进 CI 机械核对；各轮修复都留有实施记录（第 8 轮是复核轮，结论并入本文）；清单逐条回写状态 | `docs/README.md`；`RAG/scripts/check_docs.py` |

## 三、未完成

### 3.1 需要人工（代码已就绪，机器验不了视觉与部署）

| 事项 | 为什么还没做 | 什么时候做 |
| --- | --- | --- |
| 浏览器里过一遍新功能界面：用户管理页、换号登录后的 RAG 列表表现、问答页视觉细节 | 自动化只覆盖到"计算样式 + 交互结果"（问答页有脚本冒烟 17 项），审美与真实点击流仍需人眼 | 下次使用时顺手过一遍（约 15 分钟） |
| 部署服务器：核对 RAG 并入产物为新构建、公网时启用 nginx `auth_basic` | 属部署时动作，配置已就位（`deploy/nginx/china-war.conf` 有开关、`deploy/README.md` 有步骤） | 真正部署那次 |
| RAG demo 的 28 条评分人工复核 | 评分由 AI 代理完成（P2-22 口径），正式交付前应人工过一遍 | RAG 对外交付前 |
| **一次整本重跑 + 人工看抽取效果（A-5）** | 要真实调用大模型（计费、耗时）；且这同时就是 C-4 缓存自动失效后的首次生产运行 | **已决定往后放**：先把工程完善，需要时按 `entity-event-relation/README.md` 跑，并把产物指纹与人工结论记进实施记录 |

### 3.2 代码待办（按优先级）

| 优先级 | 事项 | 现状与为什么还没做 |
| --- | --- | --- |
| 中 | EER-5 残余：采样参数未记进产物 metadata | `LLMClient.call()` 的 `temperature` 默认 0.1，但产物 metadata 里没有 temperature / seed（`war_extraction/config.py` 亦无这两项）。学术评估引用产物时缺"采样参数"这一自证项。**版本号那一半第 11 轮 C-4 已做完**（机械派生 + 6 例用例） |
| 中 | EER-6 残余：`main.py` 仍是上帝模块 | 第 11 轮把 `dict_to_entities/events/relations` 三个转换器搬进共享模块后，从 1158 降到 **1041 行**；函数级拆分是独立工作，未动。重复**逻辑**已收敛为三处公共实现（`utils/json_payload.py` / `value_parsing.py` / `relation_rules.py`，31 例钉住） |
| 中 | EER-8 残余：语料规则仍散在 Python 里 | 「孙滨 → 孙膑」已改别名归一、别名表两处维护已收敛（第 11 轮 C-5），但规则尚未集中到 `config/` 下的数据文件，换语料仍会静默失效。**附带的 `LOW_QUALITY_PERSON_NAMES` 决策项已于 2026-09-25 收口：名单整体删除**（实测两者都在人工标注里各出现 1 次、预测 persons 里 0 次，而关系里 `吴起` 作「统帅」、`秦始皇` 作「君主」各 2 条——说明是合法角色被误丢），见[决策项收口记录](修复实施记录-决策项收口-20260925.md) |
| 低 | 前端剩余 `any`（CRUD 页与零散页面） | 问答页那批（大头）已随拆分清掉；其余按"随页面改动渐进"处理 |
| 高（条件触发） | RAG-8（干净 commit 上完整演练发布） | 部署/发布期动作：发布链路自认未在干净 commit 上完整跑过。**不公网部署就不阻塞**。**原同列的 RAG-1（公网鉴权）已于 2026-09-25 第 12 轮修复**：服务端 JWT 验签（`RAG_REQUIRE_AUTH` + 共享密钥），两条问答通道一视同仁，未开启时 health 持续告警；剩余动作是部署方的二选一配置与凭据轮换判断，见[第 12 轮实施记录](修复实施记录-第12轮-20260925.md)第十二节 |

### 3.3 决策项（不写代码，只需要定方向）

> **原先列在这里的三条已收口**（2026-09-25，用户逐条裁定）：占位词排除集**统一为宽口径**、
> `秦始皇`/`吴起` 过滤名单**删除**、`AlignmentTool`**删除**。三项都已落地并有常驻用例，
> 见[决策项收口记录](修复实施记录-决策项收口-20260925.md)。下表只剩需要定方向的三条。

| 决策 | 建议与触发条件 |
| --- | --- |
| `git filter-repo` 重写历史 | 凭据已轮换、紧迫性低。**建议暂缓**；若做需强推并通知所有协作者重新 clone |
| 问答隔离升到方案 B（JWT + RAG 服务端验签） | 触发条件是**公网部署**。现状方案 A 只解决"串记录"、不防"冒充"（同源下懂控制台者可改 uid）；本机/局域网够用 |
| token 角色版本号（改角色即强制下线） | 当前"菜单下次登录生效、接口实时生效"对单管理员够用；多管理员频繁调角色时再上 |

## 四、后续计划（按顺序，每条含验收标准）

**阶段 0：收尾 —— 已完成（第 10 轮，2026-09-25）**

- ~~0-1 删除 `backup.cmd`~~ → **已做**（用户授权）。删除前复核：全仓库扫描确认只有文档描述引用它、
  无任何代码/脚本/CI 引用。**如实说明**：该文件历史自初始提交起就在公开仓库可被取回，删除只影响当前与未来；
- ~~0-2 清理文档里的盘符路径（6 处）~~ → **已做**：正文改成 `conda activate <env>` 口径，
  「本机环境备注」章节保留为盘符路径的唯一合法位置；
- ~~0-3 顺手修正~~ → 已完成（问答页行数核准为 403 行）。

**阶段 1：EER 三件小修合批 —— 已完成（第 9 轮，2026-09-25）**

- ~~1-1 EER-15：评估定序化~~ → **已做**，且比原计划更彻底：不只错误样例，匹配本身也定序化了，
  因此**指标级**漂移一并消失；
- ~~1-2 EER-4：正则 bug~~ → **已做**（三处双反斜杠改单反斜杠）；
- ~~1-3 EER-11：缓存索引原子写~~ → **已做**。

**阶段 2：EER 工程债分批收口 —— 已完成（第 10 轮第一批、第 11 轮第二批）**

- 第 10 轮：~~EER-12 geocoding 重试退避~~、~~EER-10 评估器二选一~~、~~EER-6 重复收敛~~、
  ~~EER-7 提示词静默降级~~、~~EER-11 的 geocoding 硬编码路径~~；
- 第 11 轮：~~C-1 两份抽取编排合成单一入口（EER-7 收尾，本轮的最高优先项）~~、
  ~~C-2 路径锚定 + 配置不再静默 + 缓存 GC~~、~~C-3 仓库卫生（EER-13）~~、
  ~~C-4 提示词版本机械派生（EER-5 版本号这一半）~~、~~C-5 别名归一与别名表去重（EER-8 部分）~~、
  ~~C-6 关系类型精确相等 + 阈值敏感性分析（EER-9）~~、~~C-7 标注规范（EER-14）~~、
  ~~C-9 CI lint job 口径统一（DOC-6）~~；
- **剩余**：3.2 表里的 EER-5（采样参数）、EER-6（`main.py` 拆分）、EER-8（规则集中到 `config/`）。
  这几条都不阻塞任何事，随下一次抽取链改动一起做即可。**原先也列在这里的三处决策项已于
  2026-09-25 收口**（人名过滤名单、占位词排除集、`AlignmentTool`），见
  [决策项收口记录](修复实施记录-决策项收口-20260925.md)；
- 验收：每条改动都有自己的用例或可复现命令；`compileall` + `ruff` + 既有用例全绿（实测见第五节）。

**阶段 2.5：一次整本重跑 —— 待你决定时机（本版明确：往后放）**

- 为什么非做不可：第 11 轮 C-4 的提示词版本派生值与旧串不同，`cache/` 现有 196 条分段缓存**全部不再命中**，
  下次运行必然走真实 API。这不是 bug，是设计意图；但**这一次的代价是整本重跑**（计费、耗时）；
- 一次运行同时拿到三样东西：新 prompt_version 的实际抽取产物、A-5 的人工质量结论、
  A-6 第 1 条（占位词排除集）所需的对比基线；
- 验收：产物路径、产物指纹、评估结果（**注意口径已变，历史值不可比**）与人工结论记进实施记录；
  若评估数值与本文第一节的 714/174/385 有差，先核对产物指纹再下结论（第 9 轮的教训：别把"产物换代"误读成"匹配抖动"）。

**阶段 3：部署前**

- 在干净 commit 上完整演练一次 RAG 发布（RAG-8）；公网形态下启用 nginx `auth_basic`（RAG-1）；
  按需评估方案 B；
- 验收：发布包 smoke 通过、`/api/health` 版本与产物一致、`auth_basic` 打开后匿名访问被拒。

**阶段 4：不定时**

- `git filter-repo`（对外贡献变多前一次性处理）；
- 前端剩余 `any` 清理与类型收敛（随页面改动顺手做）；
- 3.2 表里那四条 EER 工程债。

## 五、当前测试与门禁的实测数字（2026-09-25，第 13 轮复核整改之后，本机）

> 用例增量（累计两批整改）：旧后端 110 → **199**（登录限流/撤销 12、introspection 12、
> 错误状态 22、口令策略 12、部署门禁 23、其余为既有用例调整），RAG 401 → **441**
> （撤销查询器 26 + 鉴权与路由 17），旧前端 140 → **146**。飞书机器人与抽取链未改动。
>
> **GitHub Actions 已全绿（9/9 job）**：推送第 12~14 轮改动时先红了飞书 job，
> 两个"本机绿、CI 红"的缺陷已修（测试夹具被过宽的 `.gitignore` 规则误伤、
> 渲染用例的跳过条件不完整）；随后旧后端 job 又红了一条**本轮新写的用例**
> （门禁的 `nginx -T` 校验在"装了 nginx 但站点没进配置树"的环境里假失败），已修。
> 佐证：`gh run view 36141221016`（commit `10d3f08`）。

| 层 | 命令 | 结果 |
| --- | --- | --- |
| 旧后端 | `cd backend && python -m pytest tests -q` | **199 passed**（本次实测；Python 3.8 环境 `place-name-KG`） |
| 旧后端凭证撤销接口 | `cd backend && python -m pytest tests/test_internal_introspect.py tests/test_login_guard.py -q` | **29 passed**（本次实测：introspect 12 + 登录限流 17） |
| 旧后端错误状态 | `cd backend && python -m pytest tests/test_error_status.py -q` | **22 passed**（四个蓝图的 4xx/5xx + 写接口业务码透传 + 形状保留 + 不外发异常原文） |
| 旧后端口令策略 | `cd backend && python -m pytest tests/test_password_policy.py -q` | **12 passed**（边界 / 首尾空白 / 不修改输入 / 短口令老账号仍可登录） |
| 部署鉴权门禁 | `cd backend && python -m pytest tests/test_deploy_auth_gate.py -q` | **23 passed**（真跑 `check_rag_auth.sh`：占位符与长度、与密钥扫描同口径、nginx 档、systemd 依赖） |
| 旧后端行为对照 | `python tools/snapshot_responses.py capture/diff`（67 请求） | **本轮仍未跑**（需要运行中的后端 + 本机真库，本机没有 `data/` 大制品）。本轮改的是**失败路径**的状态码与文案，成功路径一字未动；但既然动了响应契约，这次对照**必须**在有数据的环境里补一次——已记入[本轮实施记录第七节](修复实施记录-第13轮复核整改-20260925.md)。上一次完整对照（第 7 轮）为**完全一致**，共验四次 |
| 旧前端 | `pnpm test`（unit + component） | **146 passed**（14 个文件，本次实测）；`npx vue-tsc --noEmit` 0 错误（本次实测） |
| 旧前端问答页冒烟 | `node scripts/smoke-inference-page.mjs` | 第 11 轮记录为 17 项全过（含"问答历史按账号分桶落盘"），**本轮未重跑**。**前置**：首次使用或 playwright 升级后需先 `npx playwright install chromium` |
| RAG 前端 | `npm run test:unit` / `test:component` | **67** / **31 passed**（第 11 轮口径，本轮未改动 RAG 前端、未重跑）；`npm run typecheck` 通过 |
| RAG 后端 | `cd RAG && python -m pytest tests -q`（AI_Agent 环境，Python 3.11） | **441 passed**（本次实测） |
| RAG 凭证撤销查询 | `cd RAG && python -m pytest tests/test_rag_revocation.py tests/test_rag_auth.py -q` | **86 passed**（查询器 26 + 鉴权与撤销路由 60，含本轮新增的撤销策略三态与通道顺序用例） |
| 飞书机器人 | `cd feishu-bot && python -m pytest tests -q` | **297 passed**（本次实测；本轮未改动） |
| RAG 浏览器 | `npm run test:e2e:offline`（桩后端） | **15 passed / 5 skipped**（第 11 轮口径，本轮未重跑；10 个用例 × desktop/mobile 两个 project，5 次按 spec 内的 `test.skip(mobile)` 跳过）。**前置**：桩后端在 `/` 下托管 `frontend/dist`，而 `dist/` 默认是并入模式产物会全 404——离线跑要先 `npx vite build --outDir dist-plain` 再加 `E2E_DIST=dist-plain` |
| 抽取链（EER） | `cd entity-event-relation && python -m pytest tests -q`（place-name-KG 环境） | **109 passed**（12 个文件；本轮重跑确认，本轮未改动该模块） |
| 评估跨进程一致性 | 由 `tests/test_evaluator_deterministic.py` 钉住（4 颗 `PYTHONHASHSEED` 的子进程比对完整返回），含在上面 104 例内 | 通过。手跑口径（seed 0~5 递归 diff）仍见 `entity-event-relation/README.md`「快速开始 5」 |
| 静态检查 | `compileall` + `ruff check --select F821 --target-version py38`（backend + EER） | 通过（本次实测） |
| 文档检查 | `cd RAG && python scripts/check_docs.py --strict` | 通过 |
| 全仓库密钥扫描 | `python RAG/scripts/check_secrets.py --root .` | 未发现明文密钥（本次实测） |
| 部署脚本 | `bash -n deploy/scripts/*.sh` + `deploy/scripts/check_rag_auth.sh` 六种合成场景 | 语法通过；六种场景（正确配置 / 两侧密钥不同值 / 撤销查询半配 / nginx 档 auth_basic 注释 / nginx 档已启用 / 生产档 disabled）判定均符合预期 |

> RAG 的版本/数据计数等只维护在 [RAG/docs/current-status.md](../RAG/docs/current-status.md)；
> 上表是"某个时间点的实测"（本次为 2026-09-25 第 13 轮复核整改之后），随下次改动需重跑（重跑命令见各模块 README）。
> **"本次实测"= 本轮真的跑过**；沿用上一轮数字的行都写明了"本轮未重跑"，不要把它们当成当次证据。

## 六、接手须知（怎么继续）

1. **先读什么**：本文件 → [docs/README.md](README.md)（文档总索引）→
   [项目审查与修复清单](项目审查与修复清单.md)（逐条状态）→ 最近一轮
   [修复实施记录-第11轮](修复实施记录-第11轮-20260925.md)（改法与前车之鉴）。
   **没有"待执行的工作单"**：第 11 轮工作单已执行完毕、逐项回写；下一轮要做什么由本文第三、四节决定。
   第 9~11 轮的成果与本次文档整理**都已推送到 `origin/main`**（`091a1f3..bab2e3a`）。
2. **改代码前的固定动作**：
   - 改旧后端接口 → 先 `python tools/snapshot_responses.py capture --out before.json`，改完重启再 `diff`，
     必须"完全一致"或逐条解释差异。**注意它的盲区**：会调大模型的
     `/api/extract/entities-events` 被刻意跳过，抽取链的改动要靠 `backend/tests/test_extract_prompt.py`
     那种桩 LLM 用例与 `backend/tests/test_extract_replay.py` 的录制回放兜住（三层防护各自能兜什么，
     见 `snapshot_responses.py` 的注释）；
   - 改旧前端页面 → 先跑 `pnpm test`（133 例），改完再跑 + `build:check`；页面级改动加跑
     `node scripts/smoke-inference-page.mjs`；
   - 改 RAG 前端 → `npm run test:unit` + `test:component` + `typecheck`，隔离相关再跑
     `npm run test:e2e:offline`；
   - 改抽取链 → `pip install -e entity-event-relation` 后跑 `backend/tests`（43 例），再跑
     `cd entity-event-relation && python -m pytest tests -q`（109 例）；动了评估匹配逻辑，
     另按 `entity-event-relation/README.md` 的「快速开始 5」换多个 `PYTHONHASHSEED` 跑完整评估
     并递归 diff——**只跑两次不算验证**（两次可能碰巧一致，第 8 轮就是这么误判 EER-15 的）。
   - 改抽取链的**公共模块**（`utils/json_payload.py`、`value_parsing.py`、`relation_rules.py`）时
     留意一处**刻意保留**的差异：两个 JSON 策略（取第一个 vs 取最大）不可互换，
     改掉会变抽取产物。（另一处——`split_multi_value` 的宽/窄排除集不一致——已于 2026-09-25
     统一为宽口径，不再需要留意，见[决策项收口记录](修复实施记录-决策项收口-20260925.md)。）
3. **每轮收尾的固定动作**（第 4 轮定下的约定）：跑验证 → 勾选工作单 → 写当轮实施记录 →
   回写清单状态 → 最后提交"记录本身"。
4. **文档纪律**：数字只写一处（RAG 的进 `current-status.md` 并由 `check_docs.py` 核）；
   跨模块约定写在 `docs/`；过程性记录收口后压缩（先例见 `RAG/docs/CHANGELOG.md`）。
5. **两个环境别混用**：旧后端/抽取用 Python 3.8（`place-name-KG`），RAG 用 3.11（`AI_Agent`）。
6. **本机的坑**（都踩过、都记在文档里）：Git Bash 下 `/tmp` 与 Python 看到的 `/tmp` 不是一处、
   带前导斜杠的参数会被 MSYS 改写、`pkill -f app.py` 杀不掉 Windows 进程（用 `netstat -ano` +
   `taskkill /F`）、`vue-tsc -b` 掉出来的 `vite.config.js` 会盖住 `.ts`。
