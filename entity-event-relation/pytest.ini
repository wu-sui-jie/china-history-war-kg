[pytest]
# 抽取链（war_extraction）的常驻测试。在 entity-event-relation/ 下执行：
#   python -m pytest tests -q
#
# 为什么要有这套：此前 EER 模块在 CI 里只有 compileall + ruff（零用例），
# "评估可复现"这种口径没有回归保护——第 7 轮实测到同一输入两次评估结果不一致，
# 直到第 9 轮才定位并修掉（见 docs/项目审查与修复清单.md 的 EER-15）。
testpaths = tests
