[pytest]
# 抽取链（war_extraction）的常驻测试。在 entity-event-relation/ 下执行：
#   python -m pytest tests -q
#
# 为什么要有这套：EER 模块的"评估可复现"口径必须有回归保护。只跑 compileall + ruff
# 时零用例覆盖——同一输入两次评估结果不一致这类缺陷不会被 CI 拦住，只能靠人眼发现。
testpaths = tests
