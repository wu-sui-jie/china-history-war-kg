"""
关系抽取提示词模板
优化版：增加关系证据要求，与事件抽取的relations字段统一
"""

from jinja2 import Template


RELATION_EXTRACTION_PROMPT = Template("""你是一位专业的历史战争事件关系抽取专家。

## 提示词版本
{{ prompt_version }}

## 任务说明

基于已识别的事件和抽取的实体，识别以下四类关系：
1. 事件与地点的关系
2. 事件与组织的关系
3. 事件与人物的关系
4. 事件与事件的关系

**重要提示**：事件-事件关系已在事件抽取阶段识别，本阶段主要进行验证和补充。
**硬性要求**：只要事件字段或原文中已经出现地点、组织、人物，就必须为该事件生成对应关系；不要只输出事件-事件关系。
**关系丰富度要求**：不要把所有地点都写成“主战场”、所有组织都写成“参战方”、所有人物都写成“参与者”。必须根据语义区分下列细粒度关系，前端筛选依赖这些关系类型。

## 核心原则

1. **关系必须有文本依据**：所有关系必须在原文中有明确描述或强烈暗示
2. **实体必须来自已抽取列表**：三元组中的实体必须来自提供的实体列表
3. **关系类型规范化**：必须使用预定义的枚举值
4. **必须提供证据**：每个关系必须附带原文证据句子
5. **字段兜底生成**：如果原文证据句难以精确定位，但事件字段中已有 `Place`、`Aggressor`、`Defender`、`Allies`、`Commanders`、`KeyPersons`，仍要生成关系，evidence 可填写该字段对应的原文片段或事件描述

## 枚举值约束

### 1. 事件-地点关系
必须从以下选择：
- 主战场：主要交战地点
- 次要战场：次要交战地点
- 出发地：军队出发地点
- 目的地：军队目的地
- 途经地：行军经过地点
- 驻防地：军队驻扎地点
- 指挥所：指挥中心
- 补给地：后勤补给地点
- 战略要地：具有战略意义地点
- 议和地点：谈判/议和地点

字段映射兜底：
- 事件 `Place` 中的主要地点优先输出为 `主战场`
- 同一事件有多个地点时，除主要交战地点外应尽量区分为 `次要战场`、`出发地`、`目的地`、`途经地`、`驻防地`、`战略要地`、`议和地点` 等

### 2. 事件-组织关系
必须从以下选择：
- 发起方：主动发起战争的组织
- 防守方：被动防守的组织
- 支援方：提供支援的第三方组织
- 同盟方：结盟的组织
- 投降方：投降的组织
- 被俘方：被俘虏的组织
- 议和方：参与议和的组织
- 调停方：调解冲突的组织
- 参战方：一般性参与的组织

字段映射兜底：
- `Aggressor` → `发起方`
- `Defender` → `防守方`
- `Allies` → `支援方` 或 `同盟方`
- 如果原文出现投降、被俘、议和、调停等语义，要额外输出 `投降方`、`被俘方`、`议和方`、`调停方`

### 3. 事件-人物关系
必须从以下选择：
- 统帅：最高指挥官
- 将领：中级指挥官
- 谋士：军事谋士
- 君主：政治领袖
- 使者：外交使者
- 参与者：一般参战者
- 俘虏：被俘虏者
- 阵亡：战死人员
- 投降：投降人员
- 叛变：叛变人员

字段映射兜底：
- `Commanders` → `统帅` 或 `将领`
- `KeyPersons` → `谋士`、`使者` 或 `参与者`
- 如果原文出现君主身份、俘虏、阵亡、投降、叛变、可汗等语义，要额外输出 `君主`、`俘虏`、`阵亡`、`投降`、`叛变`、`可汗`

### 4. 事件-事件关系（验证补充）
**注意**：此部分主要验证事件抽取阶段识别的关系

必须从以下选择：
- 因果关系：A直接导致B（原文有"因此"、"于是"、"故"等）
- 顺承关系：A发生在B之前，时间连续（原文有"后"、"既而"、"继而"等）
- 并列关系：A与B同时发生（原文有"同时"、"与此同时"等）
- 包含关系：A包含B（原文有"分兵"、"一路...一路..."等）
- 条件关系：A是B发生的条件（原文有"若"、"一旦"等）

**输出约束补充**：
- `EventName_A` 和 `EventName_B` 必须填写事件列表中的**事件名称本身**
- `relation` 必须使用完整枚举：`因果关系`、`顺承关系`、`并列关系`、`包含关系`、`条件关系`
- 不要输出 `E1`、`E2`、`E3` 这类事件ID
- 如果原文只是弱相关、没有明确先后/因果/并列/包含/条件证据，则不要强行生成事件-事件关系

**关系判断触发词库**：
- 因果：因此、于是、故、导致、致使、引发
- 顺承：后、既而、继而、随后、之后、然后
- 并列：同时、与此同时、一边...一边...
- 包含：分兵、一路...一路...、分头、分别
- 条件：若、倘若、一旦、如果

## 输入信息

文本内容：
{{ text }}

已识别的事件列表：
{{ event_names }}

{% if place_list %}
已抽取的地点实体列表（关系中的地点必须从此列表中选择）：
{{ place_list }}
{% endif %}

{% if org_list %}
已抽取的组织实体列表（关系中的组织必须从此列表中选择）：
{{ org_list }}
{% endif %}

{% if person_list %}
已抽取的人物实体列表（关系中的人物必须从此列表中选择）：
{{ person_list }}
{% endif %}

## 实体来源约束

- 关系中的地点必须从已抽取地点列表中选择
- 关系中的组织必须从已抽取组织列表中选择
- 关系中的人物必须从已抽取人物列表中选择
- 关系中的事件必须从已识别事件列表中选择
- 严禁使用未在列表中出现的实体

## 输出格式

严格按照以下JSON格式输出（不要有任何额外文字）。
**重要**：每个关系必须包含"evidence"字段，说明原文依据。

```json
{
  "event_place_relations": [
    {
      "EventName": "事件名称",
      "relation": "主战场",
      "modern_name": "地点名称",
      "evidence": "原文中表明此关系的句子（如'与蚩尤战于涿鹿之野'）"
    }
  ],
  "event_organization_relations": [
    {
      "EventName": "事件名称",
      "relation": "发起方",
      "OrgName": "组织名称",
      "evidence": "原文中表明此关系的句子（如'黄帝乃征师诸侯'）"
    }
  ],
  "event_person_relations": [
    {
      "EventName": "事件名称",
      "relation": "统帅",
      "PersonName": "人物名称",
      "evidence": "原文中表明此关系的句子（如'黄帝与炎帝战'）"
    }
  ],
  "event_event_relations": [
    {
      "EventName_A": "源事件名称",
      "relation": "因果关系",
      "EventName_B": "目标事件名称",
      "evidence": "原文中表明此关系的句子（如'因此导致'、'后发生'）"
    }
  ]
}
```

## 多关系处理

- 一个事件与一个地点可能有多种关系（如既是`主战场`又是`战略要地`）
- 应生成多个三元组，每个表达一种关系
- 避免合并多个关系到一个三元组

## 质量控制检查清单

输出前请检查：
- [ ] 所有实体是否都来自提供的列表？
- [ ] 每个事件是否至少尝试生成了地点、组织、人物关系？
- [ ] 每个关系是否都有evidence字段？
- [ ] evidence是否包含实体名称和关联描述？
- [ ] 关系类型是否从固定枚举中选择？
- [ ] 事件-事件关系是否与事件抽取阶段一致？

【重要】只输出上述JSON格式的纯文本，不要添加```json标记，不要有任何解释。所有缺失的关系类型输出空数组[]，所有缺失的属性必须显式设置为null。
""")
