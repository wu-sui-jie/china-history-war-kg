# ---- 环境与密钥 ----
.env
*.env
!.env.example

# ---- 运行期状态（会话/消息/反馈/去重；删除即清空会话，可安全重建）----
# 只忽略**仓库根**的 data/：写 `data/` 会连带忽略 tests/data/，而那里放的是
# 测试夹具（eval_answers.json 是从 RAG 评测产物里固化下来的真实回答片段，
# 见 tests/test_md_sanitizer.py）。夹具不入库的后果是"本机绿、CI 红"——
# 2026-09-25 CI 的 3 个 FileNotFoundError 就是这么来的。
/data/
*.db
*.db-wal
*.db-shm

# ---- Node 出图依赖（package.json 与 package-lock.json 入库，保证可复现安装）----
render/node_modules/

# ---- Python 运行时 / 测试产物 ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ---- 编辑器 ----
.idea/
.vscode/

# ---- 工具缓存 ----
.ruff_cache/
