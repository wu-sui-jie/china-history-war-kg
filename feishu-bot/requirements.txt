# 飞书机器人服务依赖（独立维护，**不并入** RAG/requirements.txt）
# 安装：pip install -r feishu-bot/requirements.txt
# 可与 RAG 共用同一个 Python 3.11 环境（开发文档第二节）。

# ---- 飞书官方 SDK（WebSocket 长连接 + 消息/卡片/图片接口）----
# **锁版本**：长连接客户端对卡片回调帧的分发是 SDK 内部实现细节，升级可能静默
# 改变行为（例如旧版卡片回调帧被直接丢弃）。升级前必须回归按钮回调（需求文档风险 4）。
lark-oapi==1.7.3

# ---- HTTP 客户端（只调 RAG 一个服务；同步客户端与线程化 worker 匹配）----
httpx>=0.27,<1.0

# ---- 配置（可选）：读取 feishu-bot/.env；未安装时只读环境变量，行为仍然正确 ----
python-dotenv>=0.21

# ---- 测试 ----
pytest>=7.4
