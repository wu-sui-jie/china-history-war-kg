# 并入旧知识库系统 Web 入口（子路径反代）时的前端构建参数。
#
# 用法：npm run build:integration
# 产物 dist/ 由 RAG 后端同源发出，经旧系统的反代在 /rag/ 下访问。
#
# 独立部署不要用这个 mode：`npm run build` 走默认值（base=/，接口前缀=/api），
# 产物与并入前完全一致。
VITE_BASE_PATH=/rag/
VITE_API_BASE=/rag/api
