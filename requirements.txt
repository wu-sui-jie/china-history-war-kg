# RAG 子项目依赖
# 离线链路（F09/F11）：pydantic / python-dotenv / jieba / numpy
# 在线链路（RAGv2，F02–F06）：fastapi / uvicorn / openai / sse-starlette
pydantic>=1.10
python-dotenv>=0.21
jieba>=0.42
numpy>=1.24

# ---- 在线链路（RAGv2）----
fastapi>=0.110
uvicorn>=0.27
openai>=1.30
sse-starlette>=2.0

# ---- 向量检索（RAGv5，F04/F11）----
# 本地嵌入式向量库（PersistentClient，无需独立服务）；开发环境实测 1.3.4
chromadb>=1.3
