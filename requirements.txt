# RAG 运行依赖（离线链路 F09/F11 + 在线链路 F02–F06 + 向量检索）
# 开发/测试/审计依赖见 requirements-dev.txt
#
# 口径（2026-09-15 审核 P1-25）：本文件只列**运行服务真正需要**的库。
# 旧文件里混着 pytest（测试）与 sse-starlette（实际未使用——SSE 由
# StreamingResponse 直接输出，见 server/api.py），会让"最小部署"多装无用依赖。
pydantic>=1.10
python-dotenv>=0.21
jieba>=0.42
numpy>=1.24

# ---- 在线链路（RAGv2+，F02–F06）----
fastapi>=0.110
uvicorn>=0.27
openai>=1.30

# ---- 向量检索（RAGv5，F04/F11）----
# 本地嵌入式向量库（PersistentClient，无需独立服务）；开发环境实测 1.3.4
chromadb>=1.3
