# 旧知识库系统（backend/）运行依赖
#
# 环境：Python 3.11（四个模块统一，模块之间不再需要各建一个不同版本的环境）
#
# 本文件是「旧后端 + 离线抽取」的便捷入口，只列直接依赖、不锁版本。
# 要复现验证过的那套精确版本，用仓库根的统一锁（覆盖四个模块、带 sha256）：
#     pip install --require-hashes -r requirements.lock
# 抽取链 war_extraction 是仓库内的包、不在锁内，需要额外装：
#     pip install -e entity-event-relation
#
# 服务器部署：见 deploy/README.md（conda 环境名约定为 china-war-backend）

# ---- Web 框架与 SQLite ORM ----
Flask
flask-cors
Flask-SQLAlchemy
SQLAlchemy
Werkzeug

# ---- Neo4j 图数据库驱动 ----
py2neo

# ---- 登录鉴权 ----
PyJWT

# ---- 中文分词 ----
jieba

# ---- 大模型调用：旧版问答走本机 Ollama，openai/requests 供兼容端点使用 ----
ollama
openai
requests

# ---- backend/.env 配置读取 ----
python-dotenv

# ---- war_extraction（entity-event-relation 抽取链）与 backend 共用的模型层 ----
pydantic

# ---- 离线抽取流水线的三方依赖（backend 的 /api/extract/entities-events 与 entity-event-relation）----
pandas
Jinja2
fuzzywuzzy
openpyxl
tqdm
