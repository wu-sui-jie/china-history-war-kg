# 旧知识库系统（backend/）运行依赖
#
# 环境：Python 3.8
#   RAG 子系统（RAG/）必须用 Python 3.11 单独建一个环境（依赖见 RAG/requirements.txt）：
#   chromadb 要求 Python >= 3.10，而 Flask + py2neo 这一套按 3.8 编写，装进同一个环境必有一边跑不起来。
#
# 安装：pip install -r requirements.txt
# 服务器部署：见 deploy/README.md（conda 环境名约定为 china-war-backend）

# ---- Web 框架与 SQLite ORM ----
Flask
flask-cors
Flask-SQLAlchemy
SQLAlchemy
Werkzeug

# ---- Neo4j 图数据库驱动 ----
py2neo

# ---- 登录鉴权 ----
PyJWT

# ---- 中文分词 ----
jieba

# ---- 大模型调用：旧版问答走本机 Ollama，openai/requests 供兼容端点使用 ----
ollama
openai
requests

# ---- backend/.env 配置读取 ----
python-dotenv

# ---- war_extraction（entity-event-relation，P2-4 已打包）与 backend 共用的模型层 ----
pydantic

# ---- 离线抽取流水线的三方依赖（backend 的 /api/extract/entities-events 与 entity-event-relation）----
pandas
Jinja2
fuzzywuzzy
openpyxl
tqdm
