# 旧知识库系统（backend/）+ 离线抽取链（entity-event-relation）的运行依赖。
#
# 环境：Python 3.11（四个模块统一，见 README.md）。
# 本文件给的是**版本区间**，区间与根 pyproject.toml 的各模块声明一致——那里是权威来源，
# 两边改到其中一处时要同步。要装「验证过的那套精确版本」，用覆盖四个模块的统一锁：
#     pip install --require-hashes -r requirements.lock
# 抽取链 war_extraction 是本仓库内的包、不在锁里，需要另外装：
#     pip install -e entity-event-relation

# ---- Web 框架与 SQLite ORM ----
Flask>=2.3,<4
Flask-Cors>=5.0,<7
Flask-SQLAlchemy>=3.0,<4
SQLAlchemy>=2.0,<3
Werkzeug>=3.0,<4

# ---- Neo4j 图数据库驱动（py2neo 已停更，2021.2.4 是最后一版）----
py2neo>=2021.2.4,<2022

# ---- 登录鉴权 ----
PyJWT>=2.4,<3

# ---- 中文分词 ----
jieba>=0.42,<1

# ---- 旧版问答走本机 Ollama；对外 HTTP 调用用 requests ----
ollama>=0.5,<1
requests>=2.31,<3

# ---- 抽取链的三方依赖（backend 的 /api/extract/* 与 entity-event-relation 共用）----
openai>=1.0,<3
pydantic>=2.0,<3
python-dotenv>=0.21
Jinja2>=3.0
fuzzywuzzy>=0.18
python-Levenshtein>=0.20
tqdm>=4.60
pandas>=1.3
openpyxl>=3.0
